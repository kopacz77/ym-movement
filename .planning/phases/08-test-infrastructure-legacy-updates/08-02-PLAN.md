---
phase: 08-test-infrastructure-legacy-updates
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/admin-dashboard.spec.ts
  - tests/lesson-scheduling.spec.ts
  - tests/authentication.spec.ts
  - tests/debug-student-error.spec.ts
  - tests/student-signup.spec.ts
autonomous: true

must_haves:
  truths:
    - "All 13 existing E2E test files compile without TypeScript errors"
    - "No spec file contains an inline loginAsAdmin function definition"
    - "No spec file contains hardcoded credentials (all use testData constants or shared helpers)"
    - "The 8 spec files already importing from test-utils continue to work unchanged with the loginAsAdmin alias"
    - "lesson-scheduling.spec.ts no longer uses the wrong password 'admin123'"
    - "debug-student-error.spec.ts uses baseURL-relative paths instead of hardcoded localhost:3000"
  artifacts:
    - path: "tests/admin-dashboard.spec.ts"
      provides: "Admin dashboard tests using shared loginAsAdmin import"
      contains: "import.*test-utils"
    - path: "tests/lesson-scheduling.spec.ts"
      provides: "Lesson scheduling tests using shared loginAsAdmin import (fixed credentials)"
      contains: "import.*test-utils"
    - path: "tests/authentication.spec.ts"
      provides: "Auth tests using testData constants for credentials"
      contains: "testData.admin"
    - path: "tests/debug-student-error.spec.ts"
      provides: "Debug test using shared imports and relative URLs"
      contains: "import.*test-utils"
    - path: "tests/student-signup.spec.ts"
      provides: "Signup tests using testData constants for duplicate email check"
      contains: "testData.admin"
  key_links:
    - from: "tests/admin-dashboard.spec.ts"
      to: "tests/helpers/test-utils.ts"
      via: "import loginAsAdmin"
      pattern: "import.*loginAsAdmin.*test-utils"
    - from: "tests/lesson-scheduling.spec.ts"
      to: "tests/helpers/test-utils.ts"
      via: "import loginAsAdmin (replaces broken inline with wrong password)"
      pattern: "import.*loginAsAdmin.*test-utils"
    - from: "tests/authentication.spec.ts"
      to: "tests/helpers/test-utils.ts"
      via: "import testData for credential constants"
      pattern: "import.*testData.*test-utils"
---

<objective>
Update the 5 spec files with inline/hardcoded credentials to use shared test-utils imports, fixing the broken lesson-scheduling password and eliminating all credential duplication.

Purpose: TINF-03 requires all 13 existing tests to work with the SUPER_ADMIN role system. Eight files already import from test-utils (they work via the backward-compat alias). The remaining 5 have inline loginAsAdmin functions or hardcoded credentials that need consolidation.
Output: All 13 spec files using shared helpers/constants, no inline login functions, no hardcoded credentials.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-infrastructure-legacy-updates/08-RESEARCH.md
@.planning/phases/08-test-infrastructure-legacy-updates/08-01-SUMMARY.md

@tests/helpers/test-utils.ts
@tests/admin-dashboard.spec.ts
@tests/lesson-scheduling.spec.ts
@tests/authentication.spec.ts
@tests/debug-student-error.spec.ts
@tests/student-signup.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin-dashboard.spec.ts and lesson-scheduling.spec.ts to use shared helpers</name>
  <files>
    tests/admin-dashboard.spec.ts
    tests/lesson-scheduling.spec.ts
  </files>
  <action>
    1. **admin-dashboard.spec.ts**:
       - Remove the inline `loginAsAdmin` function definition (lines 4-10)
       - Add import: `import { loginAsAdmin } from "./helpers/test-utils";`
       - Keep all test logic unchanged -- the beforeEach already calls `loginAsAdmin(page)` which will now use the shared version

    2. **lesson-scheduling.spec.ts**:
       - Remove the inline `loginAsAdmin` function definition (lines 4-10) -- this is the one with WRONG password "admin123"
       - Remove the inline `loginAsStudent` function definition (lines 13-23) -- inconsistent with shared version
       - Add import: `import { loginAsAdmin, loginAsStudent } from "./helpers/test-utils";`
       - Keep all test logic unchanged -- the beforeEach and test bodies already call these functions by name

    CRITICAL: Only change imports and remove inline function definitions. Do NOT modify any test assertions, describes, or test bodies. The point is to swap the credential source, not change test behavior.

    AVOID: Do NOT add `test.use({ storageState: ... })` to these files yet. The storageState default in playwright.config.ts means they start authenticated, and the explicit loginAsAdmin calls in beforeEach will re-login (redundant but harmless). Migrating away from explicit login calls is a future optimization, not part of this plan.
  </action>
  <verify>
    - `grep -c "async function loginAsAdmin" tests/admin-dashboard.spec.ts` returns 0
    - `grep -c "async function loginAsAdmin" tests/lesson-scheduling.spec.ts` returns 0
    - `grep -c "admin123" tests/lesson-scheduling.spec.ts` returns 0
    - `grep "import.*loginAsAdmin.*test-utils" tests/admin-dashboard.spec.ts` finds the import
    - `grep "import.*loginAsAdmin.*test-utils" tests/lesson-scheduling.spec.ts` finds the import
    - `pnpm type-check` passes
  </verify>
  <done>
    Both files import loginAsAdmin (and loginAsStudent for lesson-scheduling) from test-utils instead of defining inline versions. The wrong "admin123" password in lesson-scheduling is eliminated. No test logic changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update authentication.spec.ts, debug-student-error.spec.ts, and student-signup.spec.ts to use testData constants</name>
  <files>
    tests/authentication.spec.ts
    tests/debug-student-error.spec.ts
    tests/student-signup.spec.ts
  </files>
  <action>
    1. **authentication.spec.ts**:
       - Add import: `import { testData } from "./helpers/test-utils";`
       - Replace all hardcoded `"admin@test.com"` with `testData.admin.email` (lines 77, 116, 168)
       - Replace all hardcoded `"ADMINPASS2025!"` with `testData.admin.password` (lines 78, 117, 169)
       - NOTE: The "Admin Login" test (line 73-86) tests the actual login flow, so using testData constants is correct (it still exercises the login page, just uses centralized credentials)
       - NOTE: The "Logout" test (line 109-134) has an inline login-if-needed block -- replace hardcoded creds there too
       - NOTE: The "Session Management" test (line 164-181) has an inline login -- replace hardcoded creds there too
       - Do NOT change the "Student Login" test (line 89-106) -- it uses a different student email and is testing a specific scenario
       - Do NOT change the "invalid credentials" test (line 49-56) -- it intentionally uses wrong credentials

    2. **debug-student-error.spec.ts**:
       - Add import: `import { testData } from "./helpers/test-utils";`
       - Replace hardcoded `"admin@test.com"` with `testData.admin.email` (line 56)
       - Replace hardcoded `"ADMINPASS2025!"` with `testData.admin.password` (line 61)
       - Replace hardcoded `"http://localhost:3000"` with `"/"` using relative paths (line 25: goto "/", line 35: goto "/student/dashboard", line 96: goto "/student/dashboard") -- the baseURL in playwright.config.ts handles the host
       - This debug spec was written for one-off debugging; the credential fix prevents future drift

    3. **student-signup.spec.ts**:
       - Add import: `import { testData } from "./helpers/test-utils";`
       - Replace hardcoded `"admin@test.com"` with `testData.admin.email` (line 80) -- this is in the "duplicate email" test that checks using an existing email
       - No other changes needed

    CRITICAL: These files use credentials inline within test bodies (not in a helper function), so we replace strings with testData references. Do NOT restructure the tests or extract login calls to beforeEach. Keep test structure identical.

    AVOID: Do NOT change test assertions, test names, or test structure. Only swap hardcoded credential strings for testData constants.
  </action>
  <verify>
    - `grep -c "ADMINPASS2025" tests/authentication.spec.ts` returns 0 (all replaced with testData.admin.password)
    - `grep -c "admin@test.com" tests/authentication.spec.ts` returns 0 (all replaced with testData.admin.email)
    - `grep -c "admin@test.com" tests/debug-student-error.spec.ts` returns 0
    - `grep -c "localhost:3000" tests/debug-student-error.spec.ts` returns 0
    - `grep -c "admin@test.com" tests/student-signup.spec.ts` returns 0
    - `grep "import.*testData.*test-utils" tests/authentication.spec.ts` finds the import
    - `grep "import.*testData.*test-utils" tests/debug-student-error.spec.ts` finds the import
    - `grep "import.*testData.*test-utils" tests/student-signup.spec.ts` finds the import
    - `pnpm type-check` passes
  </verify>
  <done>
    All 3 files import testData from test-utils and use testData.admin.email / testData.admin.password instead of hardcoded strings. debug-student-error.spec.ts uses relative URLs. No test logic, assertions, or structure changed. Combined with the 8 files already using shared imports, all 13 spec files now use centralized credentials.
  </done>
</task>

</tasks>

<verification>
1. No spec file defines its own inline loginAsAdmin: `grep -r "async function loginAsAdmin" tests/*.spec.ts` returns 0 matches
2. No spec file hardcodes "admin123": `grep -r "admin123" tests/` returns 0 matches
3. No spec file hardcodes ADMINPASS2025 directly: `grep -r '"ADMINPASS2025' tests/*.spec.ts` returns 0 matches
4. No spec file hardcodes admin@test.com directly: `grep -r '"admin@test.com"' tests/*.spec.ts` returns 0 matches
5. No spec file hardcodes localhost:3000: `grep -r "localhost:3000" tests/*.spec.ts` returns 0 matches
6. All 5 updated files import from test-utils: `grep -l "test-utils" tests/admin-dashboard.spec.ts tests/lesson-scheduling.spec.ts tests/authentication.spec.ts tests/debug-student-error.spec.ts tests/student-signup.spec.ts` returns all 5 files
7. `pnpm type-check` passes without errors
</verification>

<success_criteria>
- All 13 spec files use shared test-utils imports for credentials (either loginAsAdmin function or testData constants)
- Zero inline loginAsAdmin function definitions remain in spec files
- Zero hardcoded credential strings remain in spec files
- The broken "admin123" password in lesson-scheduling.spec.ts is eliminated
- debug-student-error.spec.ts uses relative URLs compatible with playwright baseURL
- `pnpm type-check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-infrastructure-legacy-updates/08-02-SUMMARY.md`
</output>
