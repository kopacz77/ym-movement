---
phase: 08-test-infrastructure-legacy-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/seed-test-data.ts
  - tests/auth.setup.ts
  - tests/helpers/test-utils.ts
  - playwright.config.ts
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Running `npx playwright test` triggers the setup project which seeds test data and saves storageState files before any spec runs"
    - "Test helpers provide loginAsSuperAdmin, loginAsCoach, loginAsStudent, navigateToCoachPage, and approveCoach functions"
    - "Seed script creates SUPER_ADMIN, COACH, second COACH, and STUDENT with CoachStudent relationships idempotently"
    - "storageState JSON files for super-admin, coach, and student are saved to playwright/.auth/ and gitignored"
    - "Existing loginAsAdmin export still works (backward compat alias for loginAsSuperAdmin)"
  artifacts:
    - path: "tests/helpers/seed-test-data.ts"
      provides: "Prisma-based idempotent test data seeding"
      contains: "prisma.user.upsert"
    - path: "tests/auth.setup.ts"
      provides: "Playwright setup project that seeds DB and saves storageState"
      contains: "storageState"
    - path: "tests/helpers/test-utils.ts"
      provides: "Extended test helpers with coach functions and testData constants"
      exports: ["loginAsSuperAdmin", "loginAsAdmin", "loginAsCoach", "loginAsStudent", "navigateToCoachPage", "approveCoach", "testData"]
    - path: "playwright.config.ts"
      provides: "Updated config with setup project dependencies and default storageState"
      contains: "dependencies"
  key_links:
    - from: "tests/auth.setup.ts"
      to: "tests/helpers/seed-test-data.ts"
      via: "execSync to run seed script"
      pattern: "npx tsx tests/helpers/seed-test-data.ts"
    - from: "playwright.config.ts"
      to: "tests/auth.setup.ts"
      via: "setup project with testMatch"
      pattern: "auth\\.setup\\.ts"
    - from: "playwright.config.ts"
      to: "playwright/.auth/super-admin.json"
      via: "storageState default for browser projects"
      pattern: "storageState.*super-admin\\.json"
---

<objective>
Create the test infrastructure foundation: seed script, auth setup project, extended test helpers, and updated Playwright config.

Purpose: Phases 9-11 all depend on this infrastructure. Without seeded multi-coach data, storageState auth, and coach helper functions, no new test can be written.
Output: Four new/updated files that make `npx playwright test` automatically seed the database and authenticate as all roles before running specs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-infrastructure-legacy-updates/08-RESEARCH.md

@tests/helpers/test-utils.ts
@playwright.config.ts
@prisma/schema.prisma
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed script, auth setup project, and gitignore update</name>
  <files>
    tests/helpers/seed-test-data.ts
    tests/auth.setup.ts
    .gitignore
  </files>
  <action>
    1. Create `tests/helpers/seed-test-data.ts` -- Prisma-based idempotent seed script:
       - Import PrismaClient and bcrypt
       - Create/upsert SUPER_ADMIN user (admin@test.com / ADMINPASS2025!) with role "SUPER_ADMIN"
       - Create/upsert SUPER_ADMIN's Coach record (isApproved: true, isActive: true, revenueSplitPercent: 100, skills: ["Figure Skating"], bio: "Test admin coach")
       - Create/upsert COACH user (coach@test.com / COACHPASS2025! / name "Test Coach") with role "COACH"
       - Create/upsert Coach record for COACH user (isApproved: true, isActive: true, revenueSplitPercent: 70, skills: ["Ice Dance", "Freestyle"], bio: "Test coach profile")
       - Create/upsert second COACH user (coach2@test.com / COACH2PASS2025! / name "Test Coach 2") with role "COACH"
       - Create/upsert Coach record for coach2 (isApproved: true, isActive: true, revenueSplitPercent: 65, skills: ["Pairs"], bio: "Test coach 2 profile")
       - Create/upsert STUDENT user (test.student@example.com / TestPassword123! / name "Test Student") with role "STUDENT"
       - Create/upsert Student record (phone: "555-123-4567", level: "PRELIMINARY", maxLessonsPerWeek: 2, isApproved: true, emergencyContact JSON)
       - Create/upsert CoachStudent relationship linking primary coach to student (isPrimary: true)
       - Create/upsert CoachStudent relationship linking coach2 to student (isPrimary: false)
       - Create/upsert Rink (name: "Test Ice Rink", address: "123 Ice Street, Test City, TC 12345", timezone: "America/Los_Angeles")
       - All upserts use `where: { email }` for users, `where: { userId }` for Coach, `where: { coachId_studentId }` for CoachStudent, `where: { name }` for Rink
       - Console.log progress for each entity
       - Disconnect Prisma at end
       - Wrap in async function with .catch() that logs error and exits with code 1
       - Use `import bcrypt from "bcrypt"` (not require). Use `import { PrismaClient } from "@prisma/client"`

    2. Create `tests/auth.setup.ts` -- Playwright setup project:
       - Import `test as setup` and `expect` from `@playwright/test`
       - Import `path` and `execSync` from child_process
       - Define auth file paths: `playwright/.auth/super-admin.json`, `playwright/.auth/coach.json`, `playwright/.auth/student.json` using `path.join(__dirname, "..")`
       - First setup step "seed test data": runs `execSync("npx tsx tests/helpers/seed-test-data.ts", { stdio: "inherit" })`
       - Second setup step "authenticate as super admin": goto /auth/login, fill email/password with admin@test.com / ADMINPASS2025!, click submit, waitForURL /admin/dashboard with 15s timeout, save storageState to super-admin.json path
       - Third setup step "authenticate as coach": same flow with coach@test.com / COACHPASS2025!, waitForURL /coach/dashboard, save to coach.json
       - Fourth setup step "authenticate as student": same flow with test.student@example.com / TestPassword123!, waitForURL /student/dashboard, save to student.json

    3. Add `playwright/.auth/` to `.gitignore` under the Testing section (after `test-results/`)

    AVOID: Do NOT use `require()` syntax -- use ESM imports. Do NOT use `globalSetup` in playwright config (project dependencies are the modern pattern). Do NOT hardcode port 3000 anywhere (app runs on 3100).
  </action>
  <verify>
    - `npx tsx tests/helpers/seed-test-data.ts` runs without errors (verify by running with the dev database)
    - `tests/auth.setup.ts` compiles without TypeScript errors: `npx tsc --noEmit tests/auth.setup.ts` or check via IDE
    - `.gitignore` contains `playwright/.auth/`
    - `grep -c "playwright/.auth" .gitignore` returns 1
  </verify>
  <done>
    Seed script creates all 4 user accounts + 2 coach records + 1 student record + 2 CoachStudent relationships + 1 rink idempotently. Auth setup file defines 4 setup steps (seed + 3 logins). StorageState directory is gitignored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend test-utils.ts and update playwright.config.ts</name>
  <files>
    tests/helpers/test-utils.ts
    playwright.config.ts
  </files>
  <action>
    1. Extend `tests/helpers/test-utils.ts` ADDITIVELY (do not remove or rename any existing exports):
       - Add coach and coach2 entries to the existing `testData` object:
         ```
         coach: {
           email: "coach@test.com",
           password: "COACHPASS2025!",
           name: "Test Coach",
         },
         coach2: {
           email: "coach2@test.com",
           password: "COACH2PASS2025!",
           name: "Test Coach 2",
         },
         ```
       - Rename existing `loginAsAdmin` function to `loginAsSuperAdmin` (same implementation)
       - Add backward compat alias: `export const loginAsAdmin = loginAsSuperAdmin;`
       - Add `loginAsCoach(page: Page, email?: string, password?: string)` function:
         goto /auth/login, fill email (default testData.coach.email), fill password (default testData.coach.password), click submit, waitForURL /coach/dashboard with 10s timeout
       - Add `navigateToCoachPage(page: Page, section: string)` function:
         click `a[href="/coach/${section}"], a:has-text("${section}")`, waitForURL `/coach/${section}`
       - Add `approveCoach(page: Page, coachEmail: string)` function:
         call navigateToAdminPage(page, "coaches"), find tr:has-text(coachEmail), find Approve button, click if visible, expect "Coach approved" text visible with 10s timeout

       CRITICAL: Keep ALL existing exports unchanged (loginAsStudent, createStudentAccount, navigateToAdminPage, navigateToStudentPage, createTimeSlot, bookLesson, approveStudent, verifyPayment, testResponsiveDesign, waitForApiCalls, clearTestData, generateTestEmail, generateFutureDateTime, takeScreenshot, assertNoConsoleErrors, mockApiResponse, testFormValidation). The loginAsAdmin name MUST still be exported as an alias.

    2. Update `playwright.config.ts`:
       - Add a "setup" project at the beginning of the projects array:
         ```
         {
           name: "setup",
           testMatch: /auth\.setup\.ts/,
         },
         ```
       - Add `dependencies: ["setup"]` to every existing browser project (chromium, firefox, webkit, Mobile Chrome, Mobile Safari)
       - Add `storageState: "playwright/.auth/super-admin.json"` to the `use` block of each browser project
       - Keep all other config unchanged (testDir, fullyParallel, forbidOnly, retries, workers, reporter, baseURL, trace, screenshot, video, webServer)

    AVOID: Do NOT remove or rename any existing exports from test-utils.ts. Do NOT change the baseURL or webServer config. Do NOT use globalSetup.
  </action>
  <verify>
    - `grep "loginAsSuperAdmin" tests/helpers/test-utils.ts` finds the function
    - `grep "loginAsAdmin" tests/helpers/test-utils.ts` finds the backward compat alias
    - `grep "loginAsCoach" tests/helpers/test-utils.ts` finds the coach login function
    - `grep "approveCoach" tests/helpers/test-utils.ts` finds the coach approval function
    - `grep "testData.coach" tests/helpers/test-utils.ts` finds the coach test data
    - `grep "setup" playwright.config.ts` finds the setup project
    - `grep "dependencies" playwright.config.ts` finds dependency declarations
    - `grep "storageState" playwright.config.ts` finds storageState config
    - Run `pnpm type-check` to verify no TypeScript errors across the project
  </verify>
  <done>
    test-utils.ts exports all original functions plus loginAsSuperAdmin, loginAsCoach, navigateToCoachPage, approveCoach, and extended testData with coach entries. loginAsAdmin remains as a backward compat alias. playwright.config.ts has setup project with auth.setup.ts match, all browser projects depend on setup, and all browser projects default to super-admin storageState.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "loginAsSuperAdmin\|loginAsCoach\|approveCoach\|navigateToCoachPage" tests/helpers/test-utils.ts` returns 4+
2. `grep -c "coach@test.com\|coach2@test.com" tests/helpers/seed-test-data.ts` returns 2+
3. `grep "dependencies.*setup" playwright.config.ts` shows browser projects depend on setup
4. `grep "playwright/.auth" .gitignore` shows storageState directory is gitignored
5. `pnpm type-check` passes without errors
</verification>

<success_criteria>
- Seed script `tests/helpers/seed-test-data.ts` creates SUPER_ADMIN, 2 COACHes, STUDENT, CoachStudent relationships, and Rink idempotently
- Auth setup `tests/auth.setup.ts` seeds DB then logs in as 3 roles and saves storageState files
- test-utils.ts has coach helpers and backward-compat loginAsAdmin alias
- playwright.config.ts uses setup project dependencies pattern
- All existing exports from test-utils.ts are preserved
- `pnpm type-check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-infrastructure-legacy-updates/08-01-SUMMARY.md`
</output>
