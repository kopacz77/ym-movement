---
phase: 02-coach-dashboard-profile
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/(protected)/coach/dashboard/page.tsx
  - src/features/coach/components/dashboard/CoachOverviewCards.tsx
  - src/features/coach/components/dashboard/CoachUpcomingLessons.tsx
  - src/features/coach/components/dashboard/CoachPastLessons.tsx
  - src/app/(protected)/coach/profile/page.tsx
  - src/features/coach/components/profile/CoachProfileForm.tsx
  - src/app/(protected)/coach/earnings/page.tsx
  - src/features/coach/components/earnings/EarningsOverview.tsx
  - src/features/coach/components/earnings/PaymentHistory.tsx
  - src/app/(protected)/coach/students/page.tsx
  - src/features/coach/components/students/CoachStudentList.tsx
  - src/app/(protected)/coach/schedule/page.tsx
autonomous: true

must_haves:
  truths:
    - "A coach can see their dashboard with overview cards (total students, upcoming lessons, completed this month, monthly earnings)"
    - "A coach can see their upcoming and past lessons with student names, rink, type, and price"
    - "A coach can edit their profile (bio, photo URL, skills, certifications, years of experience)"
    - "A coach can view their rates as read-only (pricing is set by super admin only)"
    - "A coach can see their earnings summary and payment history"
    - "A coach can see a list of students who have booked lessons with them"
    - "A coach can see a schedule/calendar view placeholder"
  artifacts:
    - path: "src/app/(protected)/coach/dashboard/page.tsx"
      provides: "Coach dashboard page"
      min_lines: 30
    - path: "src/features/coach/components/dashboard/CoachOverviewCards.tsx"
      provides: "Stats cards for coach dashboard"
      min_lines: 40
    - path: "src/features/coach/components/dashboard/CoachUpcomingLessons.tsx"
      provides: "Upcoming lessons list for coach"
      min_lines: 40
    - path: "src/app/(protected)/coach/profile/page.tsx"
      provides: "Coach profile edit page"
      min_lines: 20
    - path: "src/features/coach/components/profile/CoachProfileForm.tsx"
      provides: "Coach profile edit form"
      min_lines: 80
    - path: "src/app/(protected)/coach/earnings/page.tsx"
      provides: "Coach earnings page"
      min_lines: 20
    - path: "src/app/(protected)/coach/students/page.tsx"
      provides: "Coach students page"
      min_lines: 20
  key_links:
    - from: "src/features/coach/components/dashboard/CoachOverviewCards.tsx"
      to: "api.coach.dashboard.getDashboardStats"
      via: "TRPC useQuery"
      pattern: "coach\\.dashboard\\.getDashboardStats"
    - from: "src/features/coach/components/dashboard/CoachUpcomingLessons.tsx"
      to: "api.coach.dashboard.getUpcomingLessons"
      via: "TRPC useQuery"
      pattern: "coach\\.dashboard\\.getUpcomingLessons"
    - from: "src/features/coach/components/profile/CoachProfileForm.tsx"
      to: "api.coach.profile.updateProfile"
      via: "TRPC useMutation"
      pattern: "coach\\.profile\\.updateProfile"
    - from: "src/features/coach/components/earnings/EarningsOverview.tsx"
      to: "api.coach.earnings.getEarningsSummary"
      via: "TRPC useQuery"
      pattern: "coach\\.earnings\\.getEarningsSummary"
---

<objective>
Build all coach-facing pages: dashboard (with overview cards, upcoming/past lessons), profile (editable bio/skills/certs, read-only rates), earnings (summary + payment history), students (list of students who have booked), and a schedule placeholder.

Purpose: Implements CDSH-01 (coach dashboard), CMGT-02 (coach profile), CDSH-03 (coach earnings), CDSH-04 (coach students). These are the core pages a coach interacts with daily.

Output: Six working coach pages at /coach/dashboard, /coach/profile, /coach/earnings, /coach/students, /coach/schedule.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coach-dashboard-profile/02-RESEARCH.md
@.planning/phases/02-coach-dashboard-profile/02-01-SUMMARY.md
@.planning/phases/02-coach-dashboard-profile/02-02-SUMMARY.md

@src/app/(protected)/student/dashboard/page.tsx
@src/features/student/components/dashboard/UpcomingLessons.tsx
@src/features/student/components/dashboard/LessonSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coach dashboard page with overview cards and lesson lists</name>
  <files>
    src/app/(protected)/coach/dashboard/page.tsx,
    src/features/coach/components/dashboard/CoachOverviewCards.tsx,
    src/features/coach/components/dashboard/CoachUpcomingLessons.tsx,
    src/features/coach/components/dashboard/CoachPastLessons.tsx
  </files>
  <action>
    **1. CoachOverviewCards.tsx:**
    - Use `api.coach.dashboard.getDashboardStats.useQuery()` to fetch stats
    - Render 4 cards in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4):
      - Total Students (Users icon, blue accent)
      - Upcoming Lessons (Calendar icon, green accent)
      - Completed This Month (CheckCircle icon, purple accent)
      - Monthly Earnings (DollarSign icon, amber accent -- format as currency with $ and 2 decimal places)
    - Show loading skeleton while data loads
    - Each card uses shadcn Card component with CardHeader (icon + title) and CardContent (value)

    **2. CoachUpcomingLessons.tsx:**
    - Use `api.coach.dashboard.getUpcomingLessons.useQuery({ limit: 5 })`
    - Render each lesson as a card/row showing:
      - Student name (from lesson.Student.User.name, fallback "Unknown")
      - Lesson type (from lesson.type, with color-coded badge: Purple=Choreography, Blue=Private, Green=Group, Orange=Competition Prep -- use the existing pattern from CLAUDE.md)
      - Date and time (format with date-fns: "MMM d, yyyy" and "h:mm a")
      - Rink name
      - Price (format as $XX.XX)
    - Show "No upcoming lessons" empty state
    - Handle loading with skeleton

    **3. CoachPastLessons.tsx:**
    - Use `api.coach.dashboard.getPastLessons.useQuery({ limit: 5 })`
    - Same layout as upcoming lessons but with muted styling
    - Show "No past lessons" empty state

    **4. Dashboard page.tsx:**
    ```typescript
    "use client";
    export const dynamic = "force-dynamic";

    export default function CoachDashboardPage() {
      return (
        <div className="flex flex-col gap-6">
          <h1 className="text-2xl font-bold tracking-tight">Coach Dashboard</h1>
          <CoachOverviewCards />
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
            <CoachUpcomingLessons />
            <CoachPastLessons />
          </div>
        </div>
      );
    }
    ```

    Use the same error boundary pattern from the student dashboard if desired (optional -- the DebugErrorBoundary class).

    IMPORTANT: All TRPC calls use the client-side `api` import from `@/lib/trpc-client` (or wherever the project's client TRPC is imported from). Check existing student components for the correct import pattern.
  </action>
  <verify>
    - `pnpm type-check` passes
    - `pnpm lint` passes
    - Dashboard page renders at /coach/dashboard
    - Overview cards show stats (may be 0 for new coach)
    - Upcoming/past lessons show data when lessons exist
  </verify>
  <done>
    Coach dashboard page at /coach/dashboard shows 4 overview cards (students, upcoming, completed, earnings) and two lesson lists (upcoming and past). All data fetched via TRPC queries scoped to the logged-in coach.
  </done>
</task>

<task type="auto">
  <name>Task 2: Coach profile, earnings, students, and schedule pages</name>
  <files>
    src/app/(protected)/coach/profile/page.tsx,
    src/features/coach/components/profile/CoachProfileForm.tsx,
    src/app/(protected)/coach/earnings/page.tsx,
    src/features/coach/components/earnings/EarningsOverview.tsx,
    src/features/coach/components/earnings/PaymentHistory.tsx,
    src/app/(protected)/coach/students/page.tsx,
    src/features/coach/components/students/CoachStudentList.tsx,
    src/app/(protected)/coach/schedule/page.tsx
  </files>
  <action>
    **1. CoachProfileForm.tsx** (CMGT-02):
    - Fetch profile with `api.coach.profile.getProfile.useQuery()`
    - Use React Hook Form with Zod validation
    - Editable fields:
      - Bio (textarea, max 500 chars)
      - Photo URL (text input, accepts URL -- not file upload)
      - Skills (text input, comma-separated. Display current skills as tags. On save, split to array)
      - Certifications (textarea, max 1000 chars)
      - Years of Experience (number input, 0-99)
    - Read-only section "Your Lesson Rates" showing:
      - Private: $XX.XX (or "Not set")
      - Group: $XX.XX (or "Not set")
      - Choreography: $XX.XX (or "Not set")
      - Competition Prep: $XX.XX (or "Not set")
      - Small note: "Lesson rates are set by the admin. Contact your admin to update rates."
    - Read-only info: Name, Email, Member Since (from User relation)
    - Save button using `api.coach.profile.updateProfile.useMutation()`
    - Success toast on save, error toast on failure
    - Show revenue split percentage as read-only info

    **2. Profile page.tsx:**
    Simple wrapper: heading "My Profile" + CoachProfileForm component.

    **3. EarningsOverview.tsx** (CDSH-03):
    - Fetch with `api.coach.earnings.getEarningsSummary.useQuery()`
    - Display cards:
      - Total Earnings (all time, after split)
      - This Month (after split)
      - Pending Payments (count + amount)
      - Revenue Split (percentage)
    - Format all amounts as currency

    **4. PaymentHistory.tsx:**
    - Fetch with `api.coach.earnings.getPaymentHistory.useQuery()`
    - Table with columns: Date, Student, Lesson Type, Amount, Status, Payment Method
    - Use shadcn Table component
    - Status badges: COMPLETED (green), PENDING (yellow), FAILED (red)
    - Empty state: "No payment records yet"

    **5. Earnings page.tsx:**
    Heading "Earnings" + EarningsOverview + PaymentHistory components stacked.

    **6. CoachStudentList.tsx** (CDSH-04):
    - Fetch with `api.coach.students.getMyStudents.useQuery()`
    - Card or table showing each student:
      - Name, Email
      - Level
      - Total lessons with this coach
      - Active/Inactive badge
    - Empty state: "No students have booked lessons with you yet"

    **7. Students page.tsx:**
    Heading "My Students" + CoachStudentList component.

    **8. Schedule page.tsx:**
    Create a placeholder page:
    ```typescript
    "use client";
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
    import { Calendar } from "lucide-react";

    export default function CoachSchedulePage() {
      return (
        <div className="flex flex-col gap-6">
          <h1 className="text-2xl font-bold tracking-tight">My Schedule</h1>
          <Card>
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <Calendar className="h-5 w-5" />
                Schedule View
              </CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-muted-foreground">
                Your schedule calendar will be available here. Time slot management is coming in a future update.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    IMPORTANT:
    - Use PascalCase for all Prisma relation access in displayed data
    - Handle null/undefined gracefully with fallbacks (lesson.type ? ... : "Private")
    - Use date-fns for date formatting
    - Use Sonner for all toasts
    - Follow existing component patterns from student feature for layout and styling
  </action>
  <verify>
    - `pnpm type-check` passes
    - `pnpm lint` passes
    - All coach pages render: /coach/profile, /coach/earnings, /coach/students, /coach/schedule
    - Profile form loads coach data and saves updates
    - Earnings page shows summary cards and payment table
    - Students page lists students with lesson counts
  </verify>
  <done>
    Coach profile page with editable bio/skills/certs and read-only rates. Earnings page with revenue summary and payment history table. Students page with student list and lesson counts. Schedule placeholder page. All wired to TRPC queries.
  </done>
</task>

</tasks>

<verification>
- `pnpm type-check` passes
- `pnpm lint` passes
- All 5 coach pages render without errors: dashboard, profile, earnings, students, schedule
- Dashboard overview cards show correct stats from TRPC queries
- Profile form saves and loads coach data
- Earnings display applies revenue split percentage to amounts
- Students list shows students who have lessons with the coach
</verification>

<success_criteria>
1. Coach dashboard shows 4 stat cards and upcoming/past lesson lists
2. Coach profile form allows editing bio, photoUrl, skills, certifications, yearsExperience and shows rates as read-only
3. Coach earnings page shows revenue summary (with split applied) and payment history table
4. Coach students page lists students with their lesson counts
5. All pages accessible at /coach/* routes with proper layout and navigation
</success_criteria>

<output>
After completion, create `.planning/phases/02-coach-dashboard-profile/02-04-SUMMARY.md`
</output>
