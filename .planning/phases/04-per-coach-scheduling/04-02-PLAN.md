---
phase: 04-per-coach-scheduling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/components/scheduling/ScheduleHeader.tsx
  - src/features/admin/components/scheduling/ScheduleManager.tsx
  - src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx
  - src/features/admin/components/scheduling/WorkingBlockedDatesManager.tsx
  - src/hooks/useTimeSlots.ts
  - src/hooks/useCalendarEvents.ts
autonomous: true

must_haves:
  truths:
    - "Admin schedule header shows a coach selector dropdown alongside the rink selector"
    - "Selecting a coach filters time slots and blocked dates to that coach"
    - "Creating a time slot includes the selected coachId in the mutation payload"
    - "Creating a blocked date from the admin calendar includes the selected coachId"
    - "Admin calendar event titles show coach name when in All Coaches view"
    - "Two different coaches can have overlapping time slots without conflict"
  artifacts:
    - path: "src/features/admin/components/scheduling/ScheduleHeader.tsx"
      provides: "Coach selector dropdown in schedule header"
      contains: "onCoachSelect"
    - path: "src/features/admin/components/scheduling/ScheduleManager.tsx"
      provides: "selectedCoach state threaded through to all operations"
      contains: "selectedCoach"
    - path: "src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx"
      provides: "coachId prop included in onBookingSubmit payload"
      contains: "coachId"
    - path: "src/hooks/useTimeSlots.ts"
      provides: "coachId parameter passed to getTimeSlots query"
      contains: "coachId"
    - path: "src/hooks/useCalendarEvents.ts"
      provides: "Coach name in event titles when Coach data is available"
      contains: "Coach"
  key_links:
    - from: "src/features/admin/components/scheduling/ScheduleManager.tsx"
      to: "src/hooks/useTimeSlots.ts"
      via: "selectedCoach state passed as third argument"
      pattern: "useTimeSlots.*selectedCoach"
    - from: "src/features/admin/components/scheduling/ScheduleManager.tsx"
      to: "src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx"
      via: "selectedCoachId prop"
      pattern: "selectedCoachId"
    - from: "src/features/admin/components/scheduling/ScheduleManager.tsx"
      to: "createTimeSlot.mutate"
      via: "coachId included in mutation payload"
      pattern: "coachId.*selectedCoach"
---

<objective>
Add coach filtering and coach-aware operations to the admin schedule calendar.

Purpose: Admins need to filter the calendar by coach, see which coach owns each time slot, and ensure new time slots and blocked dates are associated with a specific coach. This satisfies SCHD-01 (time slots associated with specific coach) and enables multi-coach visibility for the super admin (Success Criterion 4).
Output: Admin calendar with coach selector, coach-filtered queries, coachId in all create mutations, coach names on calendar events.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-coach-scheduling/04-RESEARCH.md

@src/features/admin/components/scheduling/ScheduleManager.tsx
@src/features/admin/components/scheduling/ScheduleHeader.tsx
@src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx
@src/features/admin/components/scheduling/WorkingBlockedDatesManager.tsx
@src/hooks/useTimeSlots.ts
@src/hooks/useCalendarEvents.ts
@src/features/admin/api/queries/coach/coachManagementQueries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coach selector to admin schedule header and thread coachId through ScheduleManager</name>
  <files>
    src/features/admin/components/scheduling/ScheduleHeader.tsx
    src/features/admin/components/scheduling/ScheduleManager.tsx
    src/hooks/useTimeSlots.ts
  </files>
  <action>
    **ScheduleHeader.tsx changes:**
    1. Add coach-related props to `ScheduleHeaderProps` interface:
       ```typescript
       selectedCoachId?: string;
       onCoachSelect?: (coachId: string | undefined) => void;
       coaches?: Array<{ id: string; user: { name: string | null } }>;
       ```
    2. Add coach selector dropdown in the filter section, placed BEFORE the rink selector in the `flex-col sm:flex-row gap-2` div. Use the same `Select` pattern as the rink selector:
       - Value: `selectedCoachId || "all_coaches"`
       - OnValueChange: maps `"all_coaches"` to undefined, otherwise passes coachId
       - SelectItems: "All Coaches" option + map over coaches array showing `coach.user.name || "Unnamed Coach"`
       - Only render the coach selector if `coaches` prop is provided and `onCoachSelect` is provided
    3. Import `Users` from lucide-react for the coach icon (small users icon before "All Coaches" text is optional -- keep it simple, no icon needed).

    **useTimeSlots.ts changes:**
    1. Add `selectedCoachId?: string` as third parameter to `useTimeSlots` function signature
    2. Add `coachId` to the `getTimeSlots` query input: `...(selectedCoachId && { coachId: selectedCoachId })`
    3. No other changes needed -- the admin endpoint already accepts optional coachId

    **ScheduleManager.tsx changes:**
    1. Add state: `const [selectedCoach, setSelectedCoach] = useState<string | undefined>(undefined);`
    2. Fetch coaches list: `const { data: coachesData } = api.admin.coach.management.getAllCoaches.useQuery();` -- extract `coachesData?.coaches` for the dropdown. Filter to only approved+active coaches: `coachesData?.coaches?.filter((c: any) => c.isApproved && c.isActive)`
    3. Update `useTimeSlots` call: `useTimeSlots(dateRange, selectedRink, selectedCoach)`
    4. Update `blockedDateRanges` query to include coachId: `api.admin.schedule.getBlockedDates.useQuery({ startDate: dateRange.start, endDate: dateRange.end, ...(selectedCoach && { coachId: selectedCoach }) })`
    5. Pass coach props to ScheduleHeader: `selectedCoachId={selectedCoach}`, `onCoachSelect={setSelectedCoach}`, `coaches={activeCoaches}`
    6. Update `handleEnhancedBookingSubmit` to include coachId in the `createTimeSlot.mutate` call:
       ```typescript
       createTimeSlot.mutate({
         rinkId: bookingData.rinkId,
         startTime: startDateTime,
         endTime: endDateTime,
         maxStudents: bookingData.maxStudents,
         isActive: true,
         ...(selectedCoach && { coachId: selectedCoach }),
       }, { onSuccess: ... });
       ```
       Also add validation: if no coach is selected, show a toast error "Please select a coach before creating a time slot" and return early. This enforces SCHD-01 (every time slot associated with a specific coach).
    7. Add the `api` import from `@/lib/api` if not already present (it already is).

    IMPORTANT: Do NOT modify the ScheduleManager render output structure beyond adding coach props to ScheduleHeader. Do NOT change any other component interfaces.
  </action>
  <verify>
    Run `pnpm type-check` -- should pass.
    Verify coach selector in header: `grep "onCoachSelect" src/features/admin/components/scheduling/ScheduleHeader.tsx`
    Verify coachId in useTimeSlots: `grep "coachId" src/hooks/useTimeSlots.ts`
    Verify selectedCoach in ScheduleManager: `grep "selectedCoach" src/features/admin/components/scheduling/ScheduleManager.tsx`
  </verify>
  <done>
    Admin schedule header shows coach selector. Selecting a coach filters time slots and blocked dates. Creating a time slot requires a coach selection and includes coachId in the mutation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coachId to CompactTimeSlotDialog, WorkingBlockedDatesManager, and coach names in calendar events</name>
  <files>
    src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx
    src/features/admin/components/scheduling/WorkingBlockedDatesManager.tsx
    src/hooks/useCalendarEvents.ts
  </files>
  <action>
    **CompactTimeSlotDialog.tsx changes:**
    1. Add `selectedCoachId?: string` to `CompactTimeSlotDialogProps` interface
    2. Add `coachId` to the `onBookingSubmit` callback type: `onBookingSubmit: (bookingData: { date: Date; startTime: string; endTime: string; rinkId: string; maxStudents: number; coachId?: string }) => void`
    3. In the `handleSubmit` function, include `coachId: selectedCoachId` in the `onBookingSubmit` call
    4. Accept `selectedCoachId` in the component destructured props
    5. Do NOT add a coach selector inside this dialog -- the coach is already selected in the header. The dialog just passes it through.

    **Then update ScheduleManager.tsx** to pass `selectedCoachId={selectedCoach}` to `CompactTimeSlotDialog`:
    ```tsx
    <CompactTimeSlotDialog
      open={isCreateDialogOpen}
      onOpenChange={setIsCreateDialogOpen}
      selectedDate={selectedCalendarDate || undefined}
      selectedStartTime={selectedCalendarTime || undefined}
      selectedRinkId={selectedRink}
      selectedCoachId={selectedCoach}
      onBookingSubmit={handleEnhancedBookingSubmit}
      rinks={rinks || []}
      isLoading={createTimeSlot.isPending}
    />
    ```

    **WorkingBlockedDatesManager.tsx changes:**
    1. Add `coachId?: string` to `BlockedDatesManagerProps` interface
    2. Accept `coachId` in destructured props
    3. Pass `coachId` to the `getBlockedDates` query: `api.admin.schedule.getBlockedDates.useQuery({ ...(coachId && { coachId }) })`
    4. Pass `coachId` to the `createBlockedDate` mutation data: include `...(coachId && { coachId })` in the mutation input
    5. Do NOT change the UI layout or form fields

    **Then update ScheduleManager.tsx** to pass `coachId` to WorkingBlockedDatesManager:
    ```tsx
    travelDateBlocker={<WorkingBlockedDatesManager coachId={selectedCoach} />}
    ```

    **useCalendarEvents.ts changes:**
    1. In the event title building logic (around line 72-87), after building the base title, add coach name when the slot has Coach data and no timezone override is being used (which indicates "All Coaches" view):
       ```typescript
       // Add coach name for multi-coach view (when Coach data is available)
       if (slot.Coach?.User?.name) {
         title += ` [${slot.Coach.User.name}]`;
       }
       ```
       Place this AFTER the student count line (`title += \` (\${associatedLessons.length}/\${slot.maxStudents})\``).
    2. The `TimeSlot` type already includes optional `Coach` from Task 1 of Plan 01 (calendarUtils.ts update). If type-check shows Coach not on type, add a type assertion or ensure the import path is correct. The `TimeSlot` import comes from `@/features/admin/components/scheduling/calendarUtils`.
  </action>
  <verify>
    Run `pnpm type-check` -- should pass.
    Verify coachId in CompactTimeSlotDialog: `grep "coachId" src/features/admin/components/scheduling/CompactTimeSlotDialog.tsx`
    Verify coachId in WorkingBlockedDatesManager: `grep "coachId" src/features/admin/components/scheduling/WorkingBlockedDatesManager.tsx`
    Verify Coach in useCalendarEvents: `grep "Coach" src/hooks/useCalendarEvents.ts`
  </verify>
  <done>
    CompactTimeSlotDialog passes coachId through in its booking callback. WorkingBlockedDatesManager creates blocked dates with coachId. Calendar events display coach name in title when Coach data is available. All admin schedule operations are coach-aware.
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check` passes with zero errors
2. Admin schedule header has a coach selector dropdown
3. Selecting a coach filters time slots shown on the calendar
4. Creating a time slot without selecting a coach shows an error toast
5. Creating a time slot with a coach selected includes coachId in the payload
6. Blocked dates created through admin are associated with the selected coach
7. Calendar events show coach name in brackets when coach data is available
</verification>

<success_criteria>
- Coach selector visible in admin schedule header alongside rink selector
- Coach filter works: selecting a coach shows only that coach's time slots and blocked dates
- Time slot creation requires coach selection (validation error if no coach selected)
- Time slot creation includes coachId in the mutation
- Blocked date creation includes coachId when a coach is selected
- Calendar event titles show "[Coach Name]" when Coach data is present
- `pnpm type-check` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-coach-scheduling/04-02-SUMMARY.md`
</output>
