---
phase: 16-dress-detail-rental-request
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/wardrobe/lib/fitCheckBars.ts
autonomous: true

must_haves:
  truths:
    - "fitCheckBars.ts is a PURE module — zero React, Prisma, ctx, or async — same constraint as fitScore.ts so server+client can both call it without drift"
    - "computeFitBar returns { dimension, state, studentValue, dressMin, dressMax, markerPositionPct } where state is 'green' | 'amber' | 'red' | 'unknown' (DETAIL-02)"
    - "state=green when studentValue is inside [dressMin, dressMax] (alterable slack does not promote a strictly-inside value above green)"
    - "state=amber when studentValue is outside [dressMin, dressMax] but inside the alterable-slack-extended range (alterableSmaller adds 2cm below, alterableLarger adds 2cm above)"
    - "state=red when studentValue is outside the alterable-slack-extended range"
    - "state=unknown when studentValue OR dressMin OR dressMax is null"
    - "markerPositionPct is clamped to [0, 1] — never NaN even when dressMax === dressMin (range fallback `|| 1`)"
    - "Module imports ALTERABLE_SLACK_CM from fitScore.ts — single source of truth for slack constant"
  artifacts:
    - path: "src/features/wardrobe/lib/fitCheckBars.ts"
      provides: "Pure computeFitBar helper + FitBar / FitBarState types"
      exports: ["computeFitBar", "FitBar", "FitBarState"]
      min_lines: 45
  key_links:
    - from: "src/features/wardrobe/lib/fitCheckBars.ts"
      to: "ALTERABLE_SLACK_CM from fitScore.ts"
      via: "Single shared constant import — slack cannot drift between filter logic and bar visualization"
      pattern: "import.*ALTERABLE_SLACK_CM.*fitScore"
---

<objective>
Ship the pure helper module that powers the FitCheckCard's per-dimension green/amber/red bar visualization.

Purpose: Phase 15-02 established that fit math must live in a pure module so server and client compute identical results. The detail-page bar visualization needs a different output shape (per-dimension state + marker position) than the catalog scoreDress function, so it gets its own small module — but it MUST import the slack constant from fitScore.ts to prevent drift.

Output: A small, well-tested pure module the Wave 2 FitCheckCard consumes directly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dress-detail-rental-request/16-RESEARCH.md
@src/features/wardrobe/lib/fitScore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fitCheckBars.ts with computeFitBar + types</name>
  <files>src/features/wardrobe/lib/fitCheckBars.ts</files>
  <action>
Create the new file `src/features/wardrobe/lib/fitCheckBars.ts`.

File header comment:
```
// src/features/wardrobe/lib/fitCheckBars.ts
//
// Pure, side-effect-free fit-check bar visualization helpers for Phase 16
// FitCheckCard. Imports ALTERABLE_SLACK_CM from fitScore.ts so the slack
// behavior (alterable adds 2cm of tolerance) stays in lockstep with the
// catalog's fitsMe filter and bestFit sort.
//
// NO React, NO Prisma, NO ctx, NO async. Pure functions only.
```

Import the slack constant:
```
import { ALTERABLE_SLACK_CM } from "./fitScore";
```

Export the two types:
```
export type FitBarState = "green" | "amber" | "red" | "unknown";

export interface FitBar {
  dimension: "chest" | "waist" | "hips";
  state: FitBarState;
  studentValue: number | null;
  dressMin: number | null;
  dressMax: number | null;
  /** 0..1 normalized position of the student marker along the dress range. Null when state === "unknown". */
  markerPositionPct: number | null;
}
```

Implement the helper:
```
export function computeFitBar(args: {
  dimension: "chest" | "waist" | "hips";
  studentValue: number | null | undefined;
  dressMin: number | null;
  dressMax: number | null;
  alterableSmaller: boolean;
  alterableLarger: boolean;
}): FitBar {
  const { dimension, studentValue, dressMin, dressMax, alterableSmaller, alterableLarger } = args;

  // Unknown state — caller hasn't set the measurement or dress lacks the field
  if (studentValue == null || dressMin == null || dressMax == null) {
    return {
      dimension,
      state: "unknown",
      studentValue: studentValue ?? null,
      dressMin,
      dressMax,
      markerPositionPct: null,
    };
  }

  const slackLo = alterableSmaller ? ALTERABLE_SLACK_CM : 0;
  const slackHi = alterableLarger ? ALTERABLE_SLACK_CM : 0;

  // Classify state (green first, demote on miss)
  let state: FitBarState = "green";
  if (studentValue < dressMin || studentValue > dressMax) state = "amber";
  if (studentValue < dressMin - slackLo || studentValue > dressMax + slackHi) state = "red";

  // Marker position normalized along the dress range. Fall-back || 1 avoids div-by-zero
  // when dressMin === dressMax (collapsed range listings).
  const range = dressMax - dressMin || 1;
  const markerPositionPct = Math.max(0, Math.min(1, (studentValue - dressMin) / range));

  return { dimension, state, studentValue, dressMin, dressMax, markerPositionPct };
}
```

Verify the algorithm matches the research spec EXACTLY (Pattern 7 in 16-RESEARCH.md L380-403). Do not invent additional states or change the green→amber→red precedence. Order of checks: green is the default; amber wins over green when outside the raw range; red wins over amber when outside the slack-extended range.
  </action>
  <verify>
`npx tsc --noEmit` compiles cleanly. `npx biome check src/features/wardrobe/lib/fitCheckBars.ts` reports no errors. `grep "from \"react\"\|prisma\|@trpc\|async " src/features/wardrobe/lib/fitCheckBars.ts` returns ZERO matches (purity guard). `grep "ALTERABLE_SLACK_CM" src/features/wardrobe/lib/fitCheckBars.ts` returns one import + at least one usage.

Quick mental sanity check with the helper signatures:
- chest 90, dress 85-95, no alterable → green
- chest 97, dress 85-95, no alterable → amber
- chest 97, dress 85-95, alterableLarger=true → green-amber-red boundary check: 95+2=97, so studentValue (97) === maxWithSlack (97). Code path: 97 > 95 → amber. Then 97 > 95+2=97 is false → stays amber. ✓
- chest 100, dress 85-95, alterableLarger=true → 100 > 95 → amber; 100 > 97 → red ✓
- chest null → unknown ✓
- dressMin null → unknown ✓
  </verify>
  <done>
Pure helper module ships. computeFitBar returns the documented shape for every state. ALTERABLE_SLACK_CM is sourced from fitScore.ts via direct import (drift-proof).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx biome check src/features/wardrobe/lib/` clean
- Module exports `computeFitBar`, `FitBar`, `FitBarState`
- Module imports ALTERABLE_SLACK_CM from fitScore.ts
- File contains zero `react`, `@prisma`, `@trpc`, or `async` references (purity guard)
</verification>

<success_criteria>
- File exists at `src/features/wardrobe/lib/fitCheckBars.ts` (DETAIL-02 prep)
- computeFitBar handles green / amber / red / unknown states correctly
- ALTERABLE_SLACK_CM imported (single source of truth)
- markerPositionPct clamped to [0, 1]
- No React/Prisma/TRPC imports — module is portable to server context if ever needed
</success_criteria>

<output>
After completion, create `.planning/phases/16-dress-detail-rental-request/16-02-SUMMARY.md` per the standard summary template.
</output>
