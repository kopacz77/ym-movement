---
phase: 16-dress-detail-rental-request
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/features/wardrobe/components/detail/DressImageCarousel.tsx
  - src/features/wardrobe/components/detail/PricingTierTable.tsx
  - src/features/wardrobe/components/detail/StructuredSizeSummary.tsx
  - src/features/wardrobe/components/request/RentalStatusBadge.tsx
autonomous: true

must_haves:
  truths:
    - "DressImageCarousel renders a single in-house image carousel (~40 LOC) using only React state + arrow buttons + indicator dots — ZERO new npm dependencies (research Pattern 6)"
    - "Carousel seeds its initial index to images.findIndex(i => i.isPrimary), fallback 0 if no primary — first frame matches the catalog thumbnail (Pitfall 6)"
    - "Carousel hides arrow buttons + dots when total === 1 image (no UI chrome for single-image dresses)"
    - "Carousel renders an empty-state icon (ImageIcon, slate-300, square aspect, rounded-xl) when images.length === 0"
    - "Carousel uses raw <img> with biome-ignore lint/performance/noImgElement (Vercel Blob domain not in next.config remotePatterns — Pitfall 7)"
    - "PricingTierTable renders a vertical table-style card with three tiers (Competition / Seasonal / Purchase). Purchase row is OMITTED when purchasePrice is null. Cleaning fee + security deposit shown below the tiers (DETAIL-01)"
    - "All prices rendered via formatCurrencyFromCents (no inline cents/100 math anywhere)"
    - "StructuredSizeSummary renders four dimensions (chest/waist/hips/torso) as min-max ranges, length as a single value, alterableSmaller/alterableLarger as compact pills with cyan accent when true (DETAIL-01)"
    - "StructuredSizeSummary shows a '—' dash when a dimension's min or max is null; never crashes on missing data"
    - "RentalStatusBadge maps RentalRequestStatus + Rental paymentStatus enums to brand-palette pills using the 2026-04-26 sweep convention (emerald/cyan/amber/slate/rose) — never raw blue/green/orange/red"
  artifacts:
    - path: "src/features/wardrobe/components/detail/DressImageCarousel.tsx"
      provides: "In-house carousel component for the detail hero"
      exports: ["DressImageCarousel", "DressImageCarouselProps"]
      min_lines: 50
    - path: "src/features/wardrobe/components/detail/PricingTierTable.tsx"
      provides: "Three-tier pricing table (Competition / Seasonal / Purchase) + fees"
      exports: ["PricingTierTable"]
      min_lines: 40
    - path: "src/features/wardrobe/components/detail/StructuredSizeSummary.tsx"
      provides: "Structured size + length + alterable display"
      exports: ["StructuredSizeSummary"]
      min_lines: 40
    - path: "src/features/wardrobe/components/request/RentalStatusBadge.tsx"
      provides: "Status pill for RentalRequestStatus AND Rental.paymentStatus"
      exports: ["RentalStatusBadge"]
      min_lines: 25
  key_links:
    - from: "src/features/wardrobe/components/detail/PricingTierTable.tsx"
      to: "formatCurrencyFromCents"
      via: "Import from @/lib/utils — every price uses the same helper"
      pattern: "formatCurrencyFromCents"
    - from: "src/features/wardrobe/components/detail/DressImageCarousel.tsx"
      to: "isPrimary seed logic"
      via: "useState(Math.max(0, images.findIndex(i => i.isPrimary)))"
      pattern: "findIndex.*isPrimary"
---

<objective>
Build the four presentational primitives the dress detail page composes. All four are leaf components with concrete prop shapes — zero data fetching, zero side effects.

Purpose: Splitting these from FitCheckCard (Plan 16-04) and RequestRentalDialog (Plan 16-05) keeps each plan within the 2-3 task budget. These four can ship in parallel with 16-04 and 16-05 because they share no files.

Output: Four small React components ready for the Wave 3 page composition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dress-detail-rental-request/16-RESEARCH.md
@src/features/wardrobe/components/DressCard.tsx
@src/features/wardrobe/components/DressStatusBadge.tsx
@src/features/wardrobe/components/CategoryBadge.tsx
@src/features/wardrobe/components/BestFitBadge.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DressImageCarousel + RentalStatusBadge</name>
  <files>src/features/wardrobe/components/detail/DressImageCarousel.tsx, src/features/wardrobe/components/request/RentalStatusBadge.tsx</files>
  <action>
**A) Create `src/features/wardrobe/components/detail/DressImageCarousel.tsx`** (mkdir the `detail` subfolder first).

`"use client"` directive at top.

Imports:
- `import { ChevronLeft, ChevronRight, ImageIcon } from "lucide-react";`
- `import { useCallback, useState } from "react";`

Export the props interface:
```
export interface DressImageCarouselProps {
  images: Array<{ id: string; url: string; isPrimary: boolean; sortOrder: number }>;
  title: string;
}
```

Export the component:
```
export function DressImageCarousel({ images, title }: DressImageCarouselProps) {
  const startIdx = Math.max(0, images.findIndex(i => i.isPrimary));
  const [index, setIndex] = useState(startIdx);
  const total = images.length;

  const goPrev = useCallback(() => setIndex(i => (i - 1 + total) % total), [total]);
  const goNext = useCallback(() => setIndex(i => (i + 1) % total), [total]);

  if (total === 0) {
    return (
      <div className="flex aspect-square w-full items-center justify-center rounded-xl bg-slate-50 text-slate-300">
        <ImageIcon className="h-16 w-16" />
      </div>
    );
  }

  return (
    <div className="relative">
      <div className="relative aspect-square overflow-hidden rounded-xl bg-slate-50">
        {/* biome-ignore lint/performance/noImgElement: Vercel Blob domain not in next.config remotePatterns (deferred per 14-05 SUMMARY) */}
        <img
          src={images[index].url}
          alt={`${title} (image ${index + 1} of ${total})`}
          className="h-full w-full object-cover"
        />
        {total > 1 && (
          <>
            <button
              type="button"
              onClick={goPrev}
              aria-label="Previous image"
              className="absolute left-2 top-1/2 -translate-y-1/2 rounded-full bg-white/90 p-2 shadow hover:bg-white"
            >
              <ChevronLeft className="h-5 w-5" />
            </button>
            <button
              type="button"
              onClick={goNext}
              aria-label="Next image"
              className="absolute right-2 top-1/2 -translate-y-1/2 rounded-full bg-white/90 p-2 shadow hover:bg-white"
            >
              <ChevronRight className="h-5 w-5" />
            </button>
          </>
        )}
      </div>
      {total > 1 && (
        <div className="mt-3 flex justify-center gap-1.5">
          {images.map((img, i) => (
            <button
              key={img.id}
              type="button"
              onClick={() => setIndex(i)}
              aria-label={`Go to image ${i + 1}`}
              className={`h-1.5 w-6 rounded-full transition-colors ${i === index ? "bg-[#0891b2]" : "bg-slate-200 hover:bg-slate-300"}`}
            />
          ))}
        </div>
      )}
    </div>
  );
}
```

Active dot uses cyan `#0891b2`. Carousel image fills aspect-square rounded card.

**B) Create `src/features/wardrobe/components/request/RentalStatusBadge.tsx`** (mkdir the `request` subfolder first).

Imports:
- `import type { RentalRequestStatus, RentalPaymentStatus } from "@prisma/client";`

Confirm the Rental table's enum name by grepping schema.prisma for the column on the Rental model (the schema column is `paymentStatus` — find the enum it references). If the enum is named differently than `RentalPaymentStatus`, use the actual name.

Export a single component that accepts either enum:
```
type StatusInput =
  | { kind: "request"; status: RentalRequestStatus }
  | { kind: "rental"; status: RentalPaymentStatus | "RETURNED" | "DEPOSIT_RELEASED" };

export function RentalStatusBadge({ status }: { status: RentalRequestStatus | "AWAITING_PAYMENT" | "PAID" | "RETURNED" | "DEPOSIT_RELEASED" }) {
  // ...exhaustive Record<string, { label, bg, text }>
}
```

Pick from the 2026-04-26 brand palette (research note 11):
- PENDING (request) → cyan-50 bg, cyan-700 text, "Pending"
- APPROVED (request) → emerald-50 bg, emerald-700 text, "Approved"
- DECLINED (request) → rose-50 bg, rose-700 text, "Declined"
- CANCELED (request) → slate-100 bg, slate-600 text, "Canceled"
- CONVERTED (request) → violet-50 bg, violet-700 text, "Confirmed"
- AWAITING_PAYMENT (rental) → amber-50 bg, amber-700 text, "Awaiting Payment"
- PAID (rental) → emerald-50 bg, emerald-700 text, "Paid"
- RETURNED (rental) → cyan-50 bg, cyan-700 text, "Returned"
- DEPOSIT_RELEASED (rental) → slate-50 bg, slate-700 text, "Closed"

Use `Record<string, { label: string; bg: string; text: string }>` so adding a new enum value would force a compile error (same pattern as DressStatusBadge.tsx — read that file first for the exact convention).

Pill styling: `inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium`.
  </action>
  <verify>
`npx tsc --noEmit` clean. `npx biome check src/features/wardrobe/components/detail/ src/features/wardrobe/components/request/` clean. Visit the dev server (`pnpm dev`) and open any page that renders DressCard (e.g. /wardrobe) to confirm the new module subdirectories compile. Smoke-test the carousel by temporarily mounting it on /wardrobe/page.tsx with hardcoded `[{ id: "a", url: "...", isPrimary: false, sortOrder: 0 }, { id: "b", url: "...", isPrimary: true, sortOrder: 1 }]` — the initial frame should be image "b". Revert the smoke test before completing the task.
  </verify>
  <done>
Two components ship. Carousel handles empty + single + multi-image gracefully. Initial frame seeds from isPrimary. RentalStatusBadge covers all 5 RentalRequestStatus variants + 4 lifecycle states from the Rental table.
  </done>
</task>

<task type="auto">
  <name>Task 2: PricingTierTable + StructuredSizeSummary</name>
  <files>src/features/wardrobe/components/detail/PricingTierTable.tsx, src/features/wardrobe/components/detail/StructuredSizeSummary.tsx</files>
  <action>
**A) Create `src/features/wardrobe/components/detail/PricingTierTable.tsx`**

`"use client"` (only because the parent page is client-only; the file itself has no state).

Imports:
- `import { formatCurrencyFromCents } from "@/lib/utils";`

Export the component:
```
export interface PricingTierTableProps {
  competitionPrice: number; // cents — required
  seasonalPrice: number;    // cents — required
  purchasePrice: number | null; // cents — optional purchase tier
  securityDeposit: number;  // cents
  cleaningFee: number;      // cents
}

export function PricingTierTable({
  competitionPrice,
  seasonalPrice,
  purchasePrice,
  securityDeposit,
  cleaningFee,
}: PricingTierTableProps) {
  return (
    <div className="rounded-xl border border-slate-200 bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)]">
      <h3 className="text-xs font-bold uppercase tracking-[0.15em] text-slate-500 mb-4">Pricing</h3>
      <table className="w-full text-sm">
        <tbody className="divide-y divide-slate-100">
          <tr>
            <td className="py-3 text-slate-700">Competition rental</td>
            <td className="py-3 text-right font-semibold text-slate-900">{formatCurrencyFromCents(competitionPrice)}</td>
          </tr>
          <tr>
            <td className="py-3 text-slate-700">Seasonal rental</td>
            <td className="py-3 text-right font-semibold text-slate-900">{formatCurrencyFromCents(seasonalPrice)}</td>
          </tr>
          {purchasePrice != null && (
            <tr>
              <td className="py-3 text-slate-700">Purchase</td>
              <td className="py-3 text-right font-semibold text-slate-900">{formatCurrencyFromCents(purchasePrice)}</td>
            </tr>
          )}
        </tbody>
      </table>
      <div className="mt-4 pt-4 border-t border-slate-100 space-y-2 text-xs text-slate-500">
        <div className="flex justify-between">
          <span>Security deposit (refundable)</span>
          <span>{formatCurrencyFromCents(securityDeposit)}</span>
        </div>
        <div className="flex justify-between">
          <span>Cleaning fee</span>
          <span>{formatCurrencyFromCents(cleaningFee)}</span>
        </div>
      </div>
    </div>
  );
}
```

**B) Create `src/features/wardrobe/components/detail/StructuredSizeSummary.tsx`**

`"use client"`.

Export the component:
```
export interface StructuredSizeSummaryProps {
  dress: {
    sizeLabel: string;
    chestMinCm: number | null;
    chestMaxCm: number | null;
    waistMinCm: number | null;
    waistMaxCm: number | null;
    hipsMinCm: number | null;
    hipsMaxCm: number | null;
    torsoMinCm: number | null;
    torsoMaxCm: number | null;
    lengthCm: number | null;
    alterableSmaller: boolean;
    alterableLarger: boolean;
  };
}

function range(min: number | null, max: number | null): string {
  if (min == null && max == null) return "—";
  if (min == null) return `≤ ${max} cm`;
  if (max == null) return `≥ ${min} cm`;
  if (min === max) return `${min} cm`;
  return `${min}–${max} cm`;
}

export function StructuredSizeSummary({ dress }: StructuredSizeSummaryProps) {
  return (
    <div className="rounded-xl border border-slate-200 bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)]">
      <h3 className="text-xs font-bold uppercase tracking-[0.15em] text-slate-500 mb-4">Size — {dress.sizeLabel}</h3>
      <dl className="grid grid-cols-2 gap-4 text-sm">
        <div>
          <dt className="text-slate-500">Chest</dt>
          <dd className="font-medium text-slate-900">{range(dress.chestMinCm, dress.chestMaxCm)}</dd>
        </div>
        <div>
          <dt className="text-slate-500">Waist</dt>
          <dd className="font-medium text-slate-900">{range(dress.waistMinCm, dress.waistMaxCm)}</dd>
        </div>
        <div>
          <dt className="text-slate-500">Hips</dt>
          <dd className="font-medium text-slate-900">{range(dress.hipsMinCm, dress.hipsMaxCm)}</dd>
        </div>
        <div>
          <dt className="text-slate-500">Torso</dt>
          <dd className="font-medium text-slate-900">{range(dress.torsoMinCm, dress.torsoMaxCm)}</dd>
        </div>
        <div>
          <dt className="text-slate-500">Length</dt>
          <dd className="font-medium text-slate-900">{dress.lengthCm != null ? `${dress.lengthCm} cm` : "—"}</dd>
        </div>
      </dl>
      <div className="mt-4 flex flex-wrap gap-2">
        {dress.alterableSmaller && (
          <span className="inline-flex items-center rounded-full bg-cyan-50 px-2.5 py-0.5 text-xs font-medium text-cyan-700">
            Can be taken in
          </span>
        )}
        {dress.alterableLarger && (
          <span className="inline-flex items-center rounded-full bg-cyan-50 px-2.5 py-0.5 text-xs font-medium text-cyan-700">
            Can be let out
          </span>
        )}
        {!dress.alterableSmaller && !dress.alterableLarger && (
          <span className="text-xs text-slate-500">No alterations possible</span>
        )}
      </div>
    </div>
  );
}
```

Match the brand palette: editorial header (xs / bold / uppercase / tracking-0.15em / slate-500), card shadow `shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)]`, cyan-50 / cyan-700 alterable pills.
  </action>
  <verify>
`npx tsc --noEmit` clean. `npx biome check src/features/wardrobe/components/detail/` clean. Both components render correctly when temporarily mounted on /wardrobe/page.tsx with mock data (then revert the mount).

Boundary cases for PricingTierTable:
- purchasePrice=null → Purchase row omitted, table has 2 rows
- All prices 0 → renders $0.00 not "Free" or empty

Boundary cases for StructuredSizeSummary:
- All measurements null → all five dl rows render "—"
- alterableSmaller=false AND alterableLarger=false → "No alterations possible" text shows
- Both flags true → both pills render side-by-side
  </verify>
  <done>
Both components ship. PricingTierTable conditionally hides Purchase row. StructuredSizeSummary handles all null permutations without crashing. Brand palette (editorial headers + cyan pills + standard card shadow) is consistent with the catalog DressCard.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx biome check src/features/wardrobe/components/` clean
- All four files exist and export their named components
- DressImageCarousel: empty / single / multi states all render correctly
- PricingTierTable: Purchase row hides when purchasePrice null
- StructuredSizeSummary: null-safe across every dimension
- RentalStatusBadge: all 5 RentalRequestStatus + 4 lifecycle states mapped
</verification>

<success_criteria>
- All four primitives use only existing dependencies (no new pnpm adds)
- Carousel seeds initial frame from isPrimary (Pitfall 6)
- Carousel uses biome-ignore comment for raw <img> (Pitfall 7)
- All currency rendered via formatCurrencyFromCents
- Brand palette: editorial uppercase headers, cyan #0891b2 active states, standard card shadow
- Components are pure presentational — zero TRPC, zero side effects, ready to compose in Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/16-dress-detail-rental-request/16-03-SUMMARY.md` per the standard summary template.
</output>
