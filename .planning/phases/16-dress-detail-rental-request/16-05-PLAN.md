---
phase: 16-dress-detail-rental-request
plan: 05
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/features/wardrobe/components/request/RequestRentalDialog.tsx
autonomous: true

must_haves:
  truths:
    - "RequestRentalDialog is a controlled modal (open/onOpenChange props) using @/components/ui/dialog wrapping Radix Dialog (REQUEST-01)"
    - "Form uses react-hook-form + zodResolver(createRequestSchema) — schema is imported DIRECTLY from src/features/wardrobe/api/queries/requestQueries.ts (single source, drift-proof — same pattern as 15-04 measurementUpdateSchema)"
    - "Rental type radio (3 options); Purchase option is HIDDEN when dress.purchasePrice === null"
    - "Date range field uses @/components/ui/calendar mode='range' inside @/components/ui/popover — same pattern as WardrobeFilterBar (15-06)"
    - "Optional competition name input + optional competition date picker (single-date Calendar inside Popover)"
    - "Required message textarea with min-length 20 / max 1000 — Zod min(20) message surfaced via RHF formState.errors"
    - "Debounced checkAvailability ping using useDebouncedState from src/lib/context-utils — same debounce hook as WardrobeFilterBar theme search (research Pattern 8)"
    - "Inline amber warning panel when checkAvailability returns available=false — message shows the conflicting window's startDate/endDate (REQUEST-02)"
    - "Submit button disabled while: form invalid OR checkAvailability reports conflict OR create mutation in-flight"
    - "On submit success: toast.success, form.reset, onOpenChange(false), utils.wardrobe.requests.mine.invalidate (so /my-rentals refreshes when user navigates there)"
    - "On submit error: toast.error with err.message — server-side BAD_REQUEST 'Window no longer available' surfaces here (Pitfall 4 defense)"
    - "form.reset() called in useEffect cleanup when open=false (Pitfall 8 — prevents stale dates between opens)"
  artifacts:
    - path: "src/features/wardrobe/components/request/RequestRentalDialog.tsx"
      provides: "Modal form for submitting a RentalRequest (REQUEST-01, REQUEST-02, REQUEST-03)"
      exports: ["RequestRentalDialog", "RequestRentalDialogProps"]
      min_lines: 220
  key_links:
    - from: "src/features/wardrobe/components/request/RequestRentalDialog.tsx"
      to: "createRequestSchema (Plan 16-01)"
      via: "Direct ESM import — same schema parses TRPC input and form values"
      pattern: "import.*createRequestSchema.*requestQueries"
    - from: "src/features/wardrobe/components/request/RequestRentalDialog.tsx"
      to: "wardrobe.requests.checkAvailability (Plan 16-01)"
      via: "api.wardrobe.requests.checkAvailability.useQuery({ dressId, startDate, endDate }, { enabled: hasValidDates })"
      pattern: "checkAvailability.useQuery"
    - from: "src/features/wardrobe/components/request/RequestRentalDialog.tsx"
      to: "wardrobe.requests.create (Plan 16-01)"
      via: "api.wardrobe.requests.create.useMutation({ onSuccess: ..., onError: ... })"
      pattern: "requests.create.useMutation"
---

<objective>
Build the RequestRentalDialog — the modal form that converts intent into a RentalRequest row.

Purpose: This is the conversion-critical UI of the entire phase. Three procedural concerns intersect here: (1) Zod schema reuse from the TRPC layer, (2) debounced availability gating against the server, (3) RHF state lifecycle across modal open/close.

Output: One self-contained controlled-modal component the detail page mounts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dress-detail-rental-request/16-RESEARCH.md
@.planning/phases/16-dress-detail-rental-request/16-01-PLAN.md
@src/features/wardrobe/components/MeasurementForm.tsx
@src/features/wardrobe/components/WardrobeFilterBar.tsx
@src/lib/context-utils.tsx
@src/components/ui/dialog.tsx
@src/components/ui/calendar.tsx
@src/components/ui/popover.tsx
@src/components/ui/button.tsx
@src/components/ui/label.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RequestRentalDialog with schema reuse + RHF + debounced availability</name>
  <files>src/features/wardrobe/components/request/RequestRentalDialog.tsx</files>
  <action>
Before starting, READ:
1. `src/features/wardrobe/components/WardrobeFilterBar.tsx` L40-100 — the date-range Popover + Calendar mode="range" pattern. Mirror it exactly.
2. `src/features/wardrobe/components/MeasurementForm.tsx` L60-110 — the RHF + zodResolver + form.reset pattern.
3. `src/lib/context-utils.tsx` L155-175 — the `useDebouncedState` hook signature.
4. `src/components/ui/dialog.tsx` — confirm exported Dialog primitives (DialogContent, DialogHeader, DialogTitle, DialogFooter).

Create `src/features/wardrobe/components/request/RequestRentalDialog.tsx`.

`"use client"` at top.

Imports:
```
import { zodResolver } from "@hookform/resolvers/zod";
import { RentalType } from "@prisma/client";
import { format } from "date-fns";
import { AlertTriangle, CalendarIcon, Loader2 } from "lucide-react";
import { useEffect } from "react";
import type { DateRange } from "react-day-picker";
import { Controller, useForm } from "react-hook-form";
import { toast } from "sonner";
import { z } from "zod";

import { Button } from "@/components/ui/button";
import { Calendar } from "@/components/ui/calendar";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { Textarea } from "@/components/ui/textarea";
import { createRequestSchema } from "@/features/wardrobe/api/queries/requestQueries";
import { useDebouncedState } from "@/lib/context-utils";
import { api } from "@/lib/api";
```

Props:
```
export interface RequestRentalDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  dress: {
    id: string;
    title: string;
    purchasePrice: number | null; // controls Purchase option visibility
  };
}
```

Form values type: `z.infer<typeof createRequestSchema>`. Use the same `useForm<z.input<typeof createRequestSchema>, unknown, z.output<typeof createRequestSchema>>()` generics pattern from 14-04 / 15-04 ADRs (handles RHF's input/output divergence for schemas with optionals).

Inside the component:
1. RHF setup:
```
const form = useForm<z.input<typeof createRequestSchema>, unknown, z.output<typeof createRequestSchema>>({
  resolver: zodResolver(createRequestSchema),
  defaultValues: {
    dressId: dress.id,
    rentalType: RentalType.COMPETITION,
    startDate: undefined as unknown as Date,
    endDate: undefined as unknown as Date,
    competitionName: "",
    competitionDate: undefined,
    message: "",
  },
  mode: "onChange",
});
```

2. Reset on close (Pitfall 8):
```
useEffect(() => {
  if (!open) {
    form.reset();
  }
}, [open, form]);
```

3. Watch + debounce dates for the availability ping:
```
const watchedStart = form.watch("startDate");
const watchedEnd = form.watch("endDate");
const [debouncedDates, setDebouncedDates] = useDebouncedState<{ start: Date | null; end: Date | null }>(
  { start: null, end: null },
  500,
);
useEffect(() => {
  setDebouncedDates({
    start: watchedStart instanceof Date ? watchedStart : null,
    end: watchedEnd instanceof Date ? watchedEnd : null,
  });
}, [watchedStart, watchedEnd, setDebouncedDates]);

const availabilityEnabled = !!debouncedDates.start && !!debouncedDates.end && debouncedDates.end > debouncedDates.start;
const availabilityQuery = api.wardrobe.requests.checkAvailability.useQuery(
  {
    dressId: dress.id,
    startDate: debouncedDates.start ?? new Date(),
    endDate: debouncedDates.end ?? new Date(),
  },
  { enabled: availabilityEnabled, staleTime: 0 },
);
```

Verify the actual signature of `useDebouncedState` against the source file before using it. If it returns `[value, setValue]` like a useState replacement, the above shape is correct. If it returns something else (e.g., `[debouncedValue, immediateValue, setValue]`), adapt accordingly. Match exactly what's in `src/lib/context-utils.tsx`.

4. Create mutation:
```
const utils = api.useUtils();
const createMutation = api.wardrobe.requests.create.useMutation({
  onSuccess: () => {
    toast.success("Request submitted");
    form.reset();
    onOpenChange(false);
    utils.wardrobe.requests.mine.invalidate();
  },
  onError: (err) => {
    toast.error(err.message || "Failed to submit request");
  },
});
```

5. Submit handler:
```
const onSubmit = form.handleSubmit((values) => {
  createMutation.mutate(values);
});
```

6. Date range picker (mirror WardrobeFilterBar pattern):
```
<Controller
  control={form.control}
  name="startDate"
  render={({ field: startField }) => (
    <Controller
      control={form.control}
      name="endDate"
      render={({ field: endField }) => {
        const range: DateRange | undefined = startField.value || endField.value
          ? { from: startField.value as Date | undefined, to: endField.value as Date | undefined }
          : undefined;
        return (
          <Popover>
            <PopoverTrigger asChild>
              <Button type="button" variant="outline" className="w-full justify-start text-left font-normal">
                <CalendarIcon className="mr-2 h-4 w-4" />
                {range?.from && range?.to
                  ? `${format(range.from, "MMM d")} – ${format(range.to, "MMM d, yyyy")}`
                  : "Pick rental window"}
              </Button>
            </PopoverTrigger>
            <PopoverContent className="w-auto p-0" align="start">
              <Calendar
                mode="range"
                selected={range}
                onSelect={(r) => {
                  startField.onChange(r?.from);
                  endField.onChange(r?.to);
                }}
                numberOfMonths={2}
                disabled={(date) => date < new Date(new Date().setHours(0, 0, 0, 0))}
              />
            </PopoverContent>
          </Popover>
        );
      }}
    />
  )}
/>
```

7. Inline availability warning panel (REQUEST-02):
```
{availabilityQuery.data && !availabilityQuery.data.available && (
  <div className="rounded-md border border-amber-200 bg-amber-50 p-3 flex gap-2 text-sm">
    <AlertTriangle className="h-4 w-4 text-amber-600 flex-shrink-0 mt-0.5" />
    <div className="text-amber-800">
      {availabilityQuery.data.reason === "Already booked"
        ? `These dates conflict with an existing booking (${format(availabilityQuery.data.conflictStart!, "MMM d")}–${format(availabilityQuery.data.conflictEnd!, "MMM d, yyyy")}). Please pick a different window.`
        : availabilityQuery.data.reason}
    </div>
  </div>
)}
```

8. Submit button gating:
```
const conflictDetected = availabilityQuery.data && !availabilityQuery.data.available;
<Button
  type="submit"
  disabled={!form.formState.isValid || createMutation.isPending || conflictDetected || availabilityQuery.isFetching}
>
  {createMutation.isPending ? <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Submitting...</> : "Submit request"}
</Button>
```

9. Rental type radio: Show 3 options (Competition / Seasonal / Purchase) as `<input type="radio" name="rentalType">` styled buttons. Conditionally hide the Purchase option:
```
{dress.purchasePrice != null && (
  <RadioOption value={RentalType.PURCHASE} label="Purchase" />
)}
```

Layout: `<Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-lg">` with:
- DialogHeader / DialogTitle ("Request to rent {dress.title}") / DialogDescription
- form (space-y-4):
  - rental type radio group
  - date range Popover trigger
  - optional competition name input
  - optional competition date Popover (single-date Calendar mode="single")
  - required message textarea + char count + min-length helper
  - availability warning panel (conditional)
- DialogFooter with Cancel + Submit buttons

Brand palette: cyan #0891b2 on focus / submit button background; amber-50/amber-800 on warning panel; rose-600 on validation error text.
  </action>
  <verify>
`npx tsc --noEmit` clean. `npx biome check src/features/wardrobe/components/request/RequestRentalDialog.tsx` clean.

Smoke test (revert before completing):
1. Add a temporary `<RequestRentalDialog open={true} onOpenChange={() => {}} dress={{ id: "test", title: "Test", purchasePrice: null }} />` to /wardrobe/page.tsx
2. Visit http://localhost:3100/wardrobe — modal should appear
3. Confirm: 2 rental type options visible (Purchase hidden because purchasePrice null)
4. Date picker opens, allows range selection
5. Submit button is disabled until form valid
6. Toggling form values triggers RHF validation
7. Revert mount before finishing the task

If `wardrobe.requests.checkAvailability` is callable client-side without errors, the wire-up is good.
  </verify>
  <done>
RequestRentalDialog ships. Schema reused from Plan 16-01 — drift impossible. Debounced availability check renders inline warning. Form resets on close. Submit toast + invalidate fire on success. Server-side BAD_REQUEST surfaces via onError.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx biome check src/features/wardrobe/components/request/RequestRentalDialog.tsx` clean
- File exports `RequestRentalDialog` and `RequestRentalDialogProps`
- Imports `createRequestSchema` directly from Plan 16-01's queries file
- Uses `useDebouncedState` from `@/lib/context-utils`
- Form resets via useEffect when `open=false`
- Purchase option only renders when `dress.purchasePrice != null`
- Availability warning panel is conditional on `!data.available`
- Submit button disabled while invalid / pending / conflicted / availability-fetching
</verification>

<success_criteria>
- REQUEST-01: Modal collects rental type, date range, optional competition info, required message
- REQUEST-02: Inline warning on conflicting dates
- REQUEST-03 (client-half): Submit calls api.wardrobe.requests.create — server-half handles RentalRequest insert + notification
- Schema single-sourced from requestQueries.ts (no duplicate definitions)
- Brand palette: cyan submit button, amber warning panel
</success_criteria>

<output>
After completion, create `.planning/phases/16-dress-detail-rental-request/16-05-SUMMARY.md` per the standard summary template.
</output>
