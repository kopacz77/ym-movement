---
phase: 16-dress-detail-rental-request
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - src/features/wardrobe/components/detail/FitCheckCard.tsx
autonomous: true

must_haves:
  truths:
    - "FitCheckCard renders three horizontal bars (chest/waist/hips) using computeFitBar from Plan 16-02 — server and client compute identical fit math (DETAIL-02)"
    - "When NONE of chest/waist/hips measurements are set on the caller, the entire card renders a 'Set your measurements' CTA linking to /wardrobe/measurements (graceful fallback per Pattern 7)"
    - "Bar colors use the 2026-04-26 brand palette — emerald-500 (green) / amber-500 (amber) / rose-500 (red) — NEVER raw green/orange/red"
    - "Each bar shows: dimension label, dress min-max range as a tinted band, student value as a marker dot positioned via markerPositionPct, and a state-colored bar fill"
    - "State='unknown' rows render the dimension in muted slate with '—' instead of a value (do not hide the row — preserve layout symmetry)"
    - "Card matches the standard luxury shadow + border treatment used by PricingTierTable / StructuredSizeSummary (visual coherence)"
  artifacts:
    - path: "src/features/wardrobe/components/detail/FitCheckCard.tsx"
      provides: "Per-dimension fit visualization (DETAIL-02)"
      exports: ["FitCheckCard"]
      min_lines: 90
  key_links:
    - from: "src/features/wardrobe/components/detail/FitCheckCard.tsx"
      to: "computeFitBar (Plan 16-02)"
      via: "Import from @/features/wardrobe/lib/fitCheckBars — single source of truth for state classification"
      pattern: "import.*computeFitBar.*fitCheckBars"
---

<objective>
Ship the FitCheckCard — the centerpiece of DETAIL-02. Renders three per-dimension bars showing how well the caller fits each dimension of the dress, with green/amber/red color coding.

Purpose: This is the single most important "fit confidence" signal in the entire wardrobe flow. The catalog gives a score; the detail page shows the breakdown. Students decide to submit a request largely based on this card.

Output: One component, consuming Plan 16-02's pure helper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dress-detail-rental-request/16-RESEARCH.md
@.planning/phases/16-dress-detail-rental-request/16-02-PLAN.md
@src/features/wardrobe/components/BestFitBadge.tsx
@src/features/wardrobe/lib/fitScore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FitCheckCard component</name>
  <files>src/features/wardrobe/components/detail/FitCheckCard.tsx</files>
  <action>
Create `src/features/wardrobe/components/detail/FitCheckCard.tsx`.

`"use client"` at top.

Imports:
- `import Link from "next/link";`
- `import { Ruler } from "lucide-react";`
- `import { computeFitBar, type FitBar } from "@/features/wardrobe/lib/fitCheckBars";`

Props interface:
```
export interface FitCheckCardProps {
  dress: {
    chestMinCm: number | null;
    chestMaxCm: number | null;
    waistMinCm: number | null;
    waistMaxCm: number | null;
    hipsMinCm: number | null;
    hipsMaxCm: number | null;
    alterableSmaller: boolean;
    alterableLarger: boolean;
  };
  /** Caller's stored measurements; null when /wardrobe/measurements hasn't been filled. */
  measurements: {
    chestCm: number | null;
    waistCm: number | null;
    hipsCm: number | null;
  } | null;
}
```

Component skeleton:
```
export function FitCheckCard({ dress, measurements }: FitCheckCardProps) {
  // Determine if caller has ANY measurement set
  const hasAnyMeasurement =
    (measurements?.chestCm ?? null) != null ||
    (measurements?.waistCm ?? null) != null ||
    (measurements?.hipsCm ?? null) != null;

  // Fallback: no measurements set — show CTA
  if (!hasAnyMeasurement) {
    return (
      <div className="rounded-xl border border-slate-200 bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)]">
        <h3 className="text-xs font-bold uppercase tracking-[0.15em] text-slate-500 mb-3">Fit Check</h3>
        <div className="flex flex-col items-start gap-3">
          <div className="flex items-center gap-2 text-slate-700">
            <Ruler className="h-5 w-5 text-[#0891b2]" />
            <span className="text-sm">Set your measurements to see how this dress fits you.</span>
          </div>
          <Link
            href="/wardrobe/measurements"
            className="inline-flex items-center rounded-md bg-[#0891b2] px-3 py-1.5 text-sm font-medium text-white hover:bg-cyan-700"
          >
            Set measurements
          </Link>
        </div>
      </div>
    );
  }

  const bars: FitBar[] = [
    computeFitBar({
      dimension: "chest",
      studentValue: measurements?.chestCm,
      dressMin: dress.chestMinCm,
      dressMax: dress.chestMaxCm,
      alterableSmaller: dress.alterableSmaller,
      alterableLarger: dress.alterableLarger,
    }),
    computeFitBar({
      dimension: "waist",
      studentValue: measurements?.waistCm,
      dressMin: dress.waistMinCm,
      dressMax: dress.waistMaxCm,
      alterableSmaller: dress.alterableSmaller,
      alterableLarger: dress.alterableLarger,
    }),
    computeFitBar({
      dimension: "hips",
      studentValue: measurements?.hipsCm,
      dressMin: dress.hipsMinCm,
      dressMax: dress.hipsMaxCm,
      alterableSmaller: dress.alterableSmaller,
      alterableLarger: dress.alterableLarger,
    }),
  ];

  return (
    <div className="rounded-xl border border-slate-200 bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)]">
      <h3 className="text-xs font-bold uppercase tracking-[0.15em] text-slate-500 mb-4">Fit Check</h3>
      <ul className="space-y-4">
        {bars.map(bar => <FitBarRow key={bar.dimension} bar={bar} />)}
      </ul>
    </div>
  );
}

const DIMENSION_LABEL: Record<FitBar["dimension"], string> = {
  chest: "Chest",
  waist: "Waist",
  hips: "Hips",
};

const STATE_BAR_COLOR: Record<FitBar["state"], string> = {
  green: "bg-emerald-500",
  amber: "bg-amber-500",
  red: "bg-rose-500",
  unknown: "bg-slate-200",
};

const STATE_LABEL: Record<FitBar["state"], string> = {
  green: "Great fit",
  amber: "Tight fit",
  red: "Likely won't fit",
  unknown: "Not set",
};

function FitBarRow({ bar }: { bar: FitBar }) {
  const isUnknown = bar.state === "unknown";

  return (
    <li className="space-y-2">
      <div className="flex items-baseline justify-between text-sm">
        <span className="font-medium text-slate-700">{DIMENSION_LABEL[bar.dimension]}</span>
        <span className={isUnknown ? "text-slate-400" : "text-slate-900 font-medium"}>
          {isUnknown
            ? "—"
            : `${bar.studentValue} cm vs ${bar.dressMin}–${bar.dressMax} cm`}
        </span>
      </div>
      <div className="relative h-2 w-full rounded-full bg-slate-100 overflow-hidden">
        <div className={`h-full ${STATE_BAR_COLOR[bar.state]}`} style={{
          width: isUnknown ? "100%" : "100%",
          opacity: isUnknown ? 0.4 : 1,
        }} />
        {!isUnknown && bar.markerPositionPct != null && (
          <div
            className="absolute top-1/2 -translate-y-1/2 -translate-x-1/2 h-3 w-3 rounded-full border-2 border-white bg-slate-900 shadow"
            style={{ left: `${bar.markerPositionPct * 100}%` }}
          />
        )}
      </div>
      <div className={`text-xs ${isUnknown ? "text-slate-400" : bar.state === "green" ? "text-emerald-600" : bar.state === "amber" ? "text-amber-600" : "text-rose-600"}`}>
        {STATE_LABEL[bar.state]}
      </div>
    </li>
  );
}
```

Three-state coloring uses the brand sweep palette: emerald (good), amber (warning), rose (bad), slate (unknown). The bar fill spans full width — color encodes state; the marker dot encodes the student's position within the dress's range.

Match the card chrome (border-slate-200, bg-white, shadow, editorial header) to PricingTierTable and StructuredSizeSummary.
  </action>
  <verify>
`npx tsc --noEmit` clean. `npx biome check src/features/wardrobe/components/detail/FitCheckCard.tsx` clean.

Test scenarios by temporarily mounting on /wardrobe/page.tsx (revert before completing):
- measurements=null → CTA card with "Set measurements" button
- measurements={ chestCm: null, waistCm: null, hipsCm: null } → also CTA (no value set)
- measurements={ chestCm: 90, waistCm: null, hipsCm: 95 } with dress chest 85-95, hips 90-100 → chest renders green/marker on right side; waist renders unknown row; hips renders green/marker near left
- measurements={ chestCm: 100, ... } with dress chest 85-95 alterableLarger=false → red bar
- measurements={ chestCm: 96, ... } with dress chest 85-95 alterableLarger=true → amber (95 < 96 ≤ 97)
  </verify>
  <done>
FitCheckCard ships. CTA fallback works when no measurements. All four states (green/amber/red/unknown) render correctly. Pure presentational — consumes computeFitBar but does no fetching of its own.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx biome check src/features/wardrobe/components/detail/FitCheckCard.tsx` clean
- File exports `FitCheckCard`
- Consumes `computeFitBar` from Plan 16-02
- CTA fallback links to `/wardrobe/measurements`
- All state colors use brand palette (emerald/amber/rose/slate — NEVER raw green/orange/red)
</verification>

<success_criteria>
- File exists at `src/features/wardrobe/components/detail/FitCheckCard.tsx` (DETAIL-02)
- Renders three bars (chest/waist/hips) when caller has measurements
- Renders single CTA card when caller has NO measurements
- Each bar shows: dimension label, value comparison, color-coded fill, position marker, state label
- Card chrome matches the rest of the detail page (visual coherence)
</success_criteria>

<output>
After completion, create `.planning/phases/16-dress-detail-rental-request/16-04-SUMMARY.md` per the standard summary template.
</output>
