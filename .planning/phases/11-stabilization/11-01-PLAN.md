---
phase: 11-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/auth.setup.ts
  - playwright.config.ts
autonomous: true

must_haves:
  truths:
    - "auth.setup.ts runs without __dirname ReferenceError"
    - "All 222 tests are listed by Playwright without parse errors"
    - "Setup project seeds data and creates auth storageState files"
    - "Baseline test results captured showing which tests pass and which fail"
  artifacts:
    - path: "tests/auth.setup.ts"
      provides: "ESM-compatible auth setup"
      contains: "import.meta.url"
    - path: "playwright.config.ts"
      provides: "CI-ready reporter config"
      contains: "list"
  key_links:
    - from: "tests/auth.setup.ts"
      to: "playwright/.auth/*.json"
      via: "storageState file creation"
      pattern: "storageState.*path"
---

<objective>
Fix the critical __dirname blocker in auth.setup.ts that prevents ALL 222 tests from running, improve the Playwright reporter config for CI output, and run the full test suite (chromium only) to capture a baseline of pass/fail results.

Purpose: Unblock the entire test suite and establish which tests actually pass vs fail before fixing anything else.
Output: Working auth.setup.ts, CI-ready config, and a baseline test report that subsequent plans use to triage fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-stabilization/11-RESEARCH.md

@tests/auth.setup.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix __dirname ESM blocker and CI reporter config</name>
  <files>tests/auth.setup.ts, playwright.config.ts</files>
  <action>
  1. In `tests/auth.setup.ts`:
     - Add `import { fileURLToPath } from "url";` at the top
     - Add these two lines before the path constants:
       ```typescript
       const __filename = fileURLToPath(import.meta.url);
       const __dirname = path.dirname(__filename);
       ```
     - Keep all existing `path.join(__dirname, ...)` calls unchanged
     - Keep all setup functions unchanged

  2. In `playwright.config.ts`:
     - Change `reporter: "html"` to:
       ```typescript
       reporter: process.env.CI ? [["list"], ["html"]] : "html",
       ```
     - This adds console output on CI while keeping HTML for local development

  3. Verify the fix:
     - Run `npx playwright test --list --project=chromium` (WITH dependencies, no --no-deps)
     - It should list all 222 tests without any __dirname error
     - The setup project should be listed as a dependency
  </action>
  <verify>
  Run `npx playwright test --list --project=chromium` and confirm:
  - No ReferenceError for __dirname
  - 222 tests listed across 18 files
  - Setup project appears in the dependency chain
  </verify>
  <done>auth.setup.ts uses import.meta.url instead of __dirname, Playwright can list all tests with dependencies enabled</done>
</task>

<task type="auto">
  <name>Task 2: Run full Chromium test suite and capture baseline results</name>
  <files>No files modified -- observation only</files>
  <action>
  1. Ensure the dev server is running (or let Playwright's webServer config start it)
  2. Run the full Chromium test suite:
     ```bash
     npx playwright test --project=chromium 2>&1 | tee /tmp/baseline-test-results.txt
     ```
  3. Parse the output and create a categorized summary:
     - Total tests run / passed / failed / skipped
     - Group failures by spec file
     - For each failing spec file, note the failure count and brief reason (timeout, selector, assertion, etc.)
     - Note any Phase 9-10 tests (coach-flows, admin-coach-management, student-booking-flow, data-isolation, role-guards) that fail -- these are high priority

  4. Record the baseline in the SUMMARY for this plan so subsequent plans know exactly what to fix.

  IMPORTANT: Do NOT fix any tests in this task. Only observe and record. The goal is a clean baseline before any fixes are applied.

  IMPORTANT: Run with `--project=chromium` only. Do NOT run firefox, webkit, or mobile projects -- stabilization targets Chromium first per research recommendations.

  NOTE: This may take several minutes. The dev server needs to start first (webServer.timeout is 120s), then 222 tests run sequentially (CI workers: undefined means parallel locally, but with potential DB contention). If tests are very slow, you can use `--workers=2` to limit parallelism.
  </action>
  <verify>
  The baseline test results file exists and contains:
  - A count of total/passed/failed/skipped tests
  - Failure details grouped by spec file
  - Clear categorization of Phase 9-10 vs legacy failures
  </verify>
  <done>Baseline test results captured and categorized, showing exactly which of the 222 tests pass and which fail in chromium project</done>
</task>

</tasks>

<verification>
1. `npx playwright test --list --project=chromium` lists all 222 tests without errors
2. auth.setup.ts contains `import.meta.url` and `fileURLToPath`
3. playwright.config.ts has CI-aware reporter configuration
4. Baseline test results are recorded with per-spec-file pass/fail counts
</verification>

<success_criteria>
- auth.setup.ts __dirname blocker is fixed
- Playwright reporter improved for CI
- Full baseline test results captured and categorized
- No tests were modified (only infrastructure fixes)
</success_criteria>

<output>
After completion, create `.planning/phases/11-stabilization/11-01-SUMMARY.md`

The SUMMARY must include the complete baseline test results table showing per-file pass/fail counts. This is the critical input for Plans 02 and 03.
</output>
