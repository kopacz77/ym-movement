---
phase: 11-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tests/student-signup.spec.ts
  - tests/e2e-complete-flow.spec.ts
  - tests/authentication.spec.ts
  - tests/helpers/test-utils.ts
autonomous: true

must_haves:
  truths:
    - "student-signup.spec.ts tests match the current signup form (name, email, phone, Radix Select for level, Radix Checkbox for consent, Turnstile bypass, no password field)"
    - "e2e-complete-flow.spec.ts signup section uses correct form selectors"
    - "authentication.spec.ts role-based access test uses student storageState and expects correct redirect URL"
    - "authentication.spec.ts student login test uses seeded student email"
    - "createStudentAccount helper in test-utils.ts matches current signup form"
  artifacts:
    - path: "tests/student-signup.spec.ts"
      provides: "Rewritten signup tests for current form"
      contains: "role.*checkbox"
    - path: "tests/e2e-complete-flow.spec.ts"
      provides: "Fixed E2E flow with correct signup selectors"
      contains: "Registration submitted"
    - path: "tests/authentication.spec.ts"
      provides: "Fixed auth tests with correct URLs and accounts"
      contains: "auth/login"
    - path: "tests/helpers/test-utils.ts"
      provides: "Updated createStudentAccount helper"
      contains: "role.*checkbox"
  key_links:
    - from: "tests/student-signup.spec.ts"
      to: "src/app/auth/signup/page.tsx"
      via: "Playwright selectors matching actual DOM elements"
      pattern: "parentConsent.*role.*checkbox"
    - from: "tests/authentication.spec.ts"
      to: "middleware.ts"
      via: "Redirect URL expectations matching middleware behavior"
      pattern: "auth/login"
---

<objective>
Fix all code-analyzed bugs (B-002 through B-006) identified in the research: rewrite student-signup.spec.ts for the current signup form, fix e2e-complete-flow.spec.ts signup section, fix authentication.spec.ts redirect URL and student account bugs, and update the createStudentAccount test helper.

Purpose: Address all known selector mismatches and incorrect assertions that were identified through code analysis before the baseline run.
Output: Four fixed test files that correctly interact with the current UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-stabilization/11-RESEARCH.md
@.planning/phases/11-stabilization/11-01-SUMMARY.md

@tests/student-signup.spec.ts
@tests/e2e-complete-flow.spec.ts
@tests/authentication.spec.ts
@tests/helpers/test-utils.ts
@src/app/auth/signup/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite student-signup.spec.ts for current form + fix createStudentAccount helper</name>
  <files>tests/student-signup.spec.ts, tests/helpers/test-utils.ts</files>
  <action>
  **Rewrite `tests/student-signup.spec.ts`** to match the current signup form at `src/app/auth/signup/page.tsx`. The current form has:
  - `input[id="name"]` -- Full Name (required)
  - `input[id="email"]` -- Email (required, type="email")
  - `input[id="phone"]` -- Phone (optional)
  - Radix `<Select>` for skating level (NOT native `<select>`)
  - Radix `<Checkbox id="parentConsent">` for parent consent (NOT native `<input type="checkbox">`)
  - Cloudflare Turnstile CAPTCHA (sitekey fallback: `1x00000000000000000000AA`)
  - NO password field (passwords set post-approval)
  - NO emergencyContact fields
  - NO maxLessonsPerWeek field (hardcoded to 3)
  - Submit button text: "Submit Registration" (not "Sign Up")
  - Success message: "Registration submitted" (not "Account created successfully")
  - After success, redirects to `/auth/login`

  **Turnstile bypass strategy:** The dev fallback site key `1x00000000000000000000AA` is a Cloudflare testing key. Try these approaches in order:
  1. First, try without intervention -- if `NEXT_PUBLIC_TURNSTILE_SITE_KEY` is not set, the code falls back to the test key which may auto-pass
  2. If Turnstile blocks, route-intercept the script: `await page.route('**/turnstile/**', route => route.abort());` -- but then the submit button will be disabled because `turnstileToken` is null. So instead:
  3. Use page.evaluate to set the turnstileToken state: After page loads, `await page.evaluate(() => { ... })` -- but React state isn't accessible this way
  4. Best approach: Route-intercept the Turnstile API response to provide a fake token. OR set `NEXT_PUBLIC_TURNSTILE_SITE_KEY` environment variable to `1x00000000000000000000AA` in the test environment and verify the widget auto-passes. The submit button check is `disabled={isLoading || !turnstileToken}`, so the token must be truthy.
  5. If none of the above work cleanly, mock the Turnstile script to auto-callback with a token:
     ```typescript
     await page.addInitScript(() => {
       (window as any).turnstile = {
         render: (_container: any, options: any) => {
           if (options.callback) options.callback('test-token');
           return 'widget-id';
         },
         execute: () => {},
         remove: () => {},
         reset: () => {},
       };
     });
     ```
     This provides the token via callback, enabling the submit button.

  **Rewrite the tests to cover:**
  1. "should display signup form with all fields" -- check name, email, phone inputs; Radix select trigger for level; parent consent checkbox; submit button with "Submit Registration" text
  2. "should interact with Radix Select for skating level" -- click trigger, select option from dropdown
  3. "should interact with Radix Checkbox for parent consent" -- click the checkbox button, verify checked state
  4. "should successfully submit registration" -- fill all fields, handle Turnstile, submit, expect "Registration submitted" toast
  5. "should validate required fields" -- submit empty, verify HTML5 required prevents submission
  6. "should have responsive design" -- mobile/tablet viewport checks
  7. "should navigate to login page" -- click login link
  8. "should validate email format" -- fill invalid email, submit

  Use `page.locator('[role="checkbox"]#parentConsent')` for the Radix Checkbox.
  Use `page.locator('[data-slot="select-trigger"]')` or `page.getByRole('combobox')` for the Radix Select trigger.
  Use `page.locator('[role="option"]').filter({ hasText: "..." })` for selecting options.

  **Update `tests/helpers/test-utils.ts`** `createStudentAccount` function:
  - Remove password fill (no password field exists)
  - Remove maxLessonsPerWeek fill (field removed)
  - Remove emergencyContact fills (fields removed)
  - Replace `page.check('input[name="parentConsent"]')` with Radix checkbox click: `page.locator('#parentConsent[role="checkbox"]').click()`
  - Replace `page.selectOption('select[name="level"]', ...)` with Radix Select interaction
  - Change success message expectation from "Account created successfully" to "Registration submitted"
  - Add Turnstile bypass via `addInitScript` mock before navigating to signup page
  </action>
  <verify>
  Run the rewritten tests:
  ```bash
  npx playwright test tests/student-signup.spec.ts --project=chromium
  ```
  All tests should pass. If Turnstile-dependent tests fail, adjust the bypass strategy.
  Also run:
  ```bash
  npx playwright test tests/student-signup.spec.ts --project=chromium --list
  ```
  to verify all tests parse correctly.
  </verify>
  <done>student-signup.spec.ts passes all tests against the current signup form, and createStudentAccount helper uses correct selectors</done>
</task>

<task type="auto">
  <name>Task 2: Fix authentication.spec.ts and e2e-complete-flow.spec.ts bugs</name>
  <files>tests/authentication.spec.ts, tests/e2e-complete-flow.spec.ts</files>
  <action>
  **Fix `tests/authentication.spec.ts`:**

  1. **B-002 (line ~161): Role-based access test** -- "should prevent student from accessing admin dashboard"
     - Problem: Uses default super-admin storageState, so `/admin` won't redirect. Expects `/auth/signin` but middleware sends to `/auth/login`.
     - Fix: Add `test.use({ storageState: 'playwright/.auth/student.json' })` to the "Role-based Access" describe block. Change URL expectation to match what middleware does when a student hits `/admin` -- likely redirects to `/student/dashboard` or `/auth/login`. Check the middleware behavior. The test description says "prevent student from accessing admin dashboard" so the student should be redirected away from admin. Use: `await expect(page).toHaveURL(/\/(student|auth)/);`
     - If middleware redirects students to their own dashboard, use `/student/dashboard`
     - If middleware redirects to login, use `/auth\/login/`

  2. **B-003 (lines 91-107): Student login test** -- "should handle student login (pending approval)"
     - Problem: Uses `student@example.com` which is not seeded. Seeded student is `test.student@example.com`.
     - Fix: Change the email to `testData.student.email` and password to `testData.student.password`. Since the seeded student is already approved, the test should redirect to `/student/dashboard`. Update the assertion: remove the `isPendingApproval` check and just verify redirect to student dashboard.
     - Alternative: If the test intent is to test a PENDING student, create a pending student in the seed or use a different approach. However, since this is a stabilization phase, fix the test to work with existing seed data. Change the test name to "should handle student login" (remove "pending approval" reference).

  3. **B-006 (same test):** Covered by the B-003 fix above.

  4. **Protected Routes tests (lines 138-153):** These tests try to access `/admin` and `/student` without auth, expecting redirect to `/auth/login`. BUT they run with default storageState (super-admin.json), which means the user IS authenticated. These tests will NOT redirect.
     - Fix: Add `test.use({ storageState: { cookies: [], origins: [] } })` to the "Protected Routes" describe block to clear auth state.
     - This uses Playwright's built-in empty storage state to simulate unauthenticated access.

  **Fix `tests/e2e-complete-flow.spec.ts`:**

  1. **B-005: Signup section (lines 13-36):** Uses old selectors (password, emergencyContact, parentConsent as native checkbox, wrong success message, native select).
     - Fix the first test ("should complete full student onboarding and lesson booking flow"):
       - Remove: `await page.fill('input[id="password"]', ...)` (no password field)
       - Remove: `await page.fill('input[name="emergencyContact.*"]', ...)` (no emergency contact)
       - Remove: `await page.fill('input[name="maxLessonsPerWeek"]', ...)` (field removed)
       - Replace: `await page.selectOption('select[name="level"]', "PRELIMINARY")` with Radix Select interaction
       - Replace: `await page.check('input[name="parentConsent"]')` with `await page.locator('#parentConsent[role="checkbox"]').click()`
       - Change success check: `"Account created successfully"` to `"Registration submitted"`
       - Add Turnstile bypass via `addInitScript` at the start of the test (before page.goto)
     - After signup success, the page redirects to `/auth/login`. The test then tries to login as the newly created student. BUT: the student is PENDING approval and has no password yet (passwords set post-approval). This means the entire flow after signup is broken by design.
     - **Decision:** Mark the signup-dependent steps with a `test.fixme()` or restructure. The realistic fix: change this test to use the already-seeded student (`test.student@example.com`) for the booking flow steps 2-7, and test signup separately. OR use `test.skip()` with a clear reason that the signup flow now requires admin approval before login is possible.
     - Best approach: Split into a signup-only assertion (verify registration submitted) and skip the login-as-new-student steps. Keep the admin/booking flow using seeded data.

  2. **Test "should handle student lesson cancellation flow" (line 133):** Uses `existing.student@example.com` which is not seeded.
     - Fix: Change to `testData.student.email` and `testData.student.password`.

  3. **Test "should verify email functionality in production" (line 283):** Same signup issues as B-005.
     - Fix: Update selectors same as test 1. Change success message. Add Turnstile bypass.
     - Note: This test creates a real account, which may not be idempotent. Consider using `test.fixme()` if the signup submission fails due to Turnstile or API issues. Or just fix selectors and let it run.
  </action>
  <verify>
  Run the fixed tests:
  ```bash
  npx playwright test tests/authentication.spec.ts --project=chromium
  npx playwright test tests/e2e-complete-flow.spec.ts --project=chromium
  ```
  For authentication.spec.ts: All 15 tests should pass (or at minimum, the ones fixed: role-based access, student login, protected routes).
  For e2e-complete-flow.spec.ts: Fixed tests should pass. Any skipped tests should show as skipped, not failed.
  </verify>
  <done>authentication.spec.ts passes with correct redirect URLs and seeded accounts; e2e-complete-flow.spec.ts signup section uses correct selectors and handles the approval-required flow</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/student-signup.spec.ts --project=chromium` -- all tests pass
2. `npx playwright test tests/authentication.spec.ts --project=chromium` -- all tests pass
3. `npx playwright test tests/e2e-complete-flow.spec.ts --project=chromium` -- passing or cleanly skipped
4. `createStudentAccount` in test-utils.ts uses Radix component selectors
5. No test uses `select[name="level"]`, `input[name="parentConsent"]`, `input[id="password"]` on the signup page
</verification>

<success_criteria>
- All 6 known bugs (B-002 through B-006) are fixed
- student-signup.spec.ts completely rewritten for current form structure
- e2e-complete-flow.spec.ts handles the approval-required signup flow
- authentication.spec.ts uses correct URLs and seeded accounts
- createStudentAccount helper works with current signup form
</success_criteria>

<output>
After completion, create `.planning/phases/11-stabilization/11-02-SUMMARY.md`

Include per-file test results showing which tests now pass.
</output>
