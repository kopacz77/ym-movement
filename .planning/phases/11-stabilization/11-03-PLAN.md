---
phase: 11-stabilization
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - tests/admin-dashboard.spec.ts
  - tests/lesson-scheduling.spec.ts
  - tests/blocked-dates-management.spec.ts
  - tests/notifications-system.spec.ts
  - tests/payments-sorting.spec.ts
  - tests/error-handling-performance.spec.ts
  - tests/ui-components.spec.ts
  - tests/payment-reminder-email.spec.ts
  - tests/reports-dashboard.spec.ts
  - tests/debug-student-error.spec.ts
  - tests/coach-flows.spec.ts
  - tests/admin-coach-management.spec.ts
  - tests/student-booking-flow.spec.ts
  - tests/data-isolation.spec.ts
  - tests/role-guards.spec.ts
autonomous: true

must_haves:
  truths:
    - "All 222 E2E tests pass or are intentionally skipped in chromium headless mode"
    - "Zero unexpected failures in the final full suite run"
    - "Phase 9-10 tests (32 tests) all pass"
    - "Legacy tests either pass or are marked test.fixme() with documented reason"
  artifacts:
    - path: "tests/coach-flows.spec.ts"
      provides: "Passing coach flow tests"
    - path: "tests/admin-coach-management.spec.ts"
      provides: "Passing admin coach management tests"
    - path: "tests/student-booking-flow.spec.ts"
      provides: "Passing student booking flow tests"
    - path: "tests/data-isolation.spec.ts"
      provides: "Passing data isolation tests"
    - path: "tests/role-guards.spec.ts"
      provides: "Passing role guard tests"
  key_links:
    - from: "npx playwright test --project=chromium"
      to: "Zero failures"
      via: "All spec files fixed or skipped"
      pattern: "0 failed"
---

<objective>
Fix all remaining test failures discovered from the baseline run (Plan 01) and the known bug fixes (Plan 02). Prioritize Phase 9-10 tests (high value), then triage and fix legacy test failures. Run the final full suite to achieve zero failures.

Purpose: Achieve the STAB-01 and STAB-02 requirements -- all bugs fixed and all tests passing in headless mode.
Output: Complete green test suite with zero unexpected failures.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-stabilization/11-RESEARCH.md
@.planning/phases/11-stabilization/11-01-SUMMARY.md
@.planning/phases/11-stabilization/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Phase 9-10 test failures and triage remaining legacy failures</name>
  <files>May touch any spec file in tests/ directory depending on failures found</files>
  <action>
  **Step 1: Run the full suite after Plan 02 fixes to get updated failure list**

  ```bash
  npx playwright test --project=chromium 2>&1 | tee /tmp/post-fix-test-results.txt
  ```

  Parse results and categorize remaining failures into:
  - **Phase 9-10 failures** (coach-flows, admin-coach-management, student-booking-flow, data-isolation, role-guards) -- fix ALL of these, they are high-value recent tests
  - **Legacy spec failures** -- triage each file

  **Step 2: Fix Phase 9-10 test failures (PRIORITY)**

  These 5 spec files (32 tests) were written against the current codebase in Phases 9-10 and should mostly pass. If any fail, the issues are likely:
  - Timing issues (add `waitForLoadState`, increase timeouts)
  - Selector changes since tests were written (update selectors)
  - Data dependencies from seed script (verify seed creates expected data)
  - Auth storageState issues (verify correct role is used)

  For each failing Phase 9-10 test:
  1. Run the individual test with `--headed` to see what happens: `npx playwright test tests/{file}.spec.ts --project=chromium --headed`
  2. Identify the failure point (timeout, selector, assertion)
  3. Fix the test to match current behavior
  4. Re-run to confirm fix

  **Step 3: Triage and fix legacy test failures**

  For each legacy spec file with failures, decide:

  A. **Quick fix** (1-3 selector or assertion changes): Fix inline.
     Common patterns:
     - Text content changed: Update expected text
     - Element moved: Update selector
     - Timing issue: Add `waitForLoadState` or increase timeout
     - Radix component: Use role-based selectors instead of native element selectors

  B. **Moderate fix** (4-8 changes needed): Fix if the tests cover important functionality.
     - admin-dashboard.spec.ts: Important -- fix it
     - lesson-scheduling.spec.ts: Important -- fix it
     - payments-sorting.spec.ts: Important -- fix it
     - reports-dashboard.spec.ts: Important -- fix it

  C. **Broken beyond repair** (fundamental mismatch with current UI): Mark with `test.fixme()` and a clear comment explaining why.
     ```typescript
     test.fixme('test name', async ({ page }) => {
       // FIXME: This test requires [specific UI feature] that was removed/changed in v1.0.
       // The underlying feature still works but the test selectors are completely outdated.
       // Rewrite needed: [brief description of what changed]
     });
     ```
     Only use this for tests where the effort to fix exceeds the value of the test. Always document the reason.

  D. **Debug/utility tests**: `debug-student-error.spec.ts` has 1 test. If it fails, mark as `test.fixme()` since it's a debug utility, not a real test.

  **Decision framework for legacy tests:**
  - If test covers CRUD operations that still exist: Fix it (category A or B)
  - If test covers UI that was fundamentally redesigned: `test.fixme()` (category C)
  - If test is flaky due to timing: Add retries or waits, don't skip
  - If test depends on specific data that doesn't exist: Update to use seeded data

  **Important guidelines:**
  - Do NOT change application code to make tests pass. Only fix test code.
  - Run tests in batches by file, not one at a time, to catch interaction effects
  - After fixing a file, re-run it individually to confirm before moving on
  - Keep track of what was fixed vs skipped for the SUMMARY
  </action>
  <verify>
  After fixing all Phase 9-10 tests:
  ```bash
  npx playwright test tests/coach-flows.spec.ts tests/admin-coach-management.spec.ts tests/student-booking-flow.spec.ts tests/data-isolation.spec.ts tests/role-guards.spec.ts --project=chromium
  ```
  All 32 Phase 9-10 tests should pass (except the 1 known intentional skip in student-booking-flow.spec.ts).

  After fixing/triaging legacy tests, run each fixed file individually to confirm.
  </verify>
  <done>All Phase 9-10 tests pass. All legacy tests either pass or are marked test.fixme() with documented reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Final full suite run -- zero failures verification</name>
  <files>No files modified -- verification only</files>
  <action>
  1. Run the complete Chromium test suite:
     ```bash
     npx playwright test --project=chromium
     ```

  2. Verify the results:
     - **Zero failures** (all tests pass or are intentionally skipped/fixme'd)
     - Count: Total tests, passed, skipped/fixme'd, failed
     - If ANY tests fail: go back and fix them. Do not accept "mostly passing."

  3. If there are unexpected failures:
     - Run the failing test individually with `--headed` to diagnose
     - Fix the test
     - Re-run the full suite again
     - Repeat until zero failures

  4. Record final results:
     - Total tests: should be 222
     - Passed: count
     - Skipped/fixme: count (with reasons)
     - Failed: MUST be 0

  5. Generate the HTML test report:
     ```bash
     npx playwright show-report
     ```
     (This opens the report in a browser for visual confirmation)

  **The milestone requirement (STAB-02) is: "All E2E tests pass in CI-compatible headless mode." This means zero failures. test.fixme() is acceptable as it marks tests as known-broken (they don't count as failures). test.skip() with a condition is also acceptable.**
  </action>
  <verify>
  ```bash
  npx playwright test --project=chromium
  ```
  Output shows: `X passed`, `Y skipped`, `0 failed`
  </verify>
  <done>Complete E2E test suite passes in headless Chromium with zero failures. STAB-01 and STAB-02 requirements met.</done>
</task>

</tasks>

<verification>
1. `npx playwright test --project=chromium` shows 0 failures
2. All Phase 9-10 tests (32 tests across 5 files) pass
3. All legacy tests either pass or are marked test.fixme() with documented reasons
4. No application code was modified (only test code)
5. HTML test report is clean
</verification>

<success_criteria>
- STAB-01: All bugs discovered during test writing are fixed (selector mismatches, redirect URLs, seeded data)
- STAB-02: All E2E tests pass in CI-compatible headless Chromium mode with zero failures
- Phase 9-10 tests: 31/32 pass (1 intentional skip)
- Legacy tests: All pass or marked test.fixme() with documented reasons
- Final suite output: 0 failed
</success_criteria>

<output>
After completion, create `.planning/phases/11-stabilization/11-03-SUMMARY.md`

Include:
- Final test suite results (total/passed/skipped/failed)
- List of test.fixme() tests with reasons
- Comparison: baseline (Plan 01) vs final results
- Confirmation that STAB-01 and STAB-02 are met
</output>
