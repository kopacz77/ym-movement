---
phase: 14-admin-inventory-crud
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - src/app/(protected)/wardrobe/page.tsx
autonomous: true

must_haves:
  truths:
    - "/wardrobe renders a graceful 'Coming soon' placeholder page so the student sidebar entry (added in Plan 14-06 NAV-01) does not 404"
    - "Page lives under the (protected) route group so it inherits authentication; non-authenticated users redirect to login"
    - "Placeholder uses editorial styling (navy #1a3a5c header, slate-500 subtitle) consistent with Phase 14 brand sweep"
    - "Placeholder explicitly states this is Phase 15 territory so the next planner has a clear hook to replace it"
  artifacts:
    - path: "src/app/(protected)/wardrobe/page.tsx"
      provides: "Student-facing placeholder route — gets replaced in Phase 15 with the actual catalog"
      min_lines: 20
  key_links:
    - from: "src/app/(protected)/wardrobe/page.tsx"
      to: "(protected) layout group"
      via: "route file location under src/app/(protected)/wardrobe/"
      pattern: "\\(protected\\)/wardrobe/page\\.tsx"
---

<objective>
Land a graceful placeholder at `/wardrobe` so the student sidebar entry added in Plan 14-06 does not 404. This is a 15-line page. Phase 15 replaces it with the real catalog browse experience.

**Purpose:** NAV-01 ("Sidebar entry routes students to /wardrobe") requires that the route resolves. Phase 15 owns the real implementation; Phase 14 owns the stub. Without this stub, clicking the new sidebar entry as a student would 404 — a regression no admin wants to ship.

**Output:** One placeholder page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-inventory-crud/14-RESEARCH.md
@CLAUDE.md

# Existing protected route to confirm the layout group provides auth gating
@src/app/(protected)/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the /wardrobe placeholder page</name>
  <files>src/app/(protected)/wardrobe/page.tsx</files>
  <action>
Create the file. First confirm the parent directory does not already exist:

```bash
ls src/app/\(protected\)/wardrobe/ 2>/dev/null
```

If it already exists, the directory check is fine — just make sure no `page.tsx` is there to clobber.

Write `src/app/(protected)/wardrobe/page.tsx`:

```tsx
import { Shirt } from "lucide-react";

export default function WardrobePlaceholderPage() {
  return (
    <div className="space-y-8">
      <div>
        <h1 className="text-3xl font-bold tracking-tight text-[#1a3a5c]">Wardrobe</h1>
        <p className="mt-1 text-sm text-slate-500">
          Browse and rent competition dresses.
        </p>
      </div>

      <div className="bg-white rounded-xl border border-slate-200 shadow-[0_1px_3px_rgba(0,0,0,0.04),0_8px_32px_rgba(0,0,0,0.02)] p-12 text-center max-w-2xl mx-auto">
        <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-[#0891b2]/10 text-[#0891b2] mb-4">
          <Shirt className="w-8 h-8" />
        </div>
        <h2 className="text-xl font-bold text-[#1a3a5c]">Coming soon</h2>
        <p className="mt-2 text-sm text-slate-600 max-w-md mx-auto">
          The wardrobe catalog launches with Phase 15. You will be able to browse competition
          dresses, filter by category and color, see fit recommendations based on your measurements,
          and submit rental requests directly to dress owners.
        </p>
      </div>
    </div>
  );
}
```

**Notes:**
- No `"use client"` directive — this is a static placeholder with no interactivity. Server-component-default keeps it lightweight.
- The route group `(protected)` does NOT have its own `layout.tsx` at the top level — let me re-check the structure. Actually, looking at the directory tree: `src/app/(protected)/admin/layout.tsx` and `src/app/(protected)/coach/layout.tsx` exist, but is there a top-level `src/app/(protected)/layout.tsx` providing auth? **Verify before assuming.** If a top-level auth-gating layout exists, this page inherits it. If not, the page is reachable without auth — which is fine for a placeholder but worth noting for Phase 15.

  Run: `ls src/app/(protected)/layout.tsx 2>/dev/null && echo "exists" || echo "no top-level layout — page renders without auth gating"`. If no top-level layout: still fine because the page is a static placeholder with no sensitive data. Phase 15 owns adding `<AppLayout role="student">` wrapping or per-page auth gating.

- For visual consistency, the placeholder uses the standard card shadow and brand cyan accent.
- The Shirt icon mirrors the sidebar icon so users recognize the surface even when empty.
- Copy explicitly mentions "Phase 15" so the next planner has zero ambiguity.

**Out of scope:**
- No AppLayout wrapping (Phase 15 will decide whether to wrap student routes in `AppLayout role="student"` consistently)
- No TRPC calls (placeholder is static)
- No tests (it is throwaway code that Phase 15 deletes)
  </action>
  <verify>
- File exists at `src/app/(protected)/wardrobe/page.tsx`
- `npx tsc --noEmit` passes
- `npx biome check src/app/\(protected\)/wardrobe/page.tsx` clean
- `grep -n "Coming soon" src/app/\(protected\)/wardrobe/page.tsx` matches
- `grep -n "Phase 15" src/app/\(protected\)/wardrobe/page.tsx` matches
- Manual smoke: `pnpm dev`, navigate to `http://localhost:3100/wardrobe` — the placeholder renders without a 404
  </verify>
  <done>
Static placeholder page exists at /wardrobe; renders an editorial header + a centered "Coming soon" card with Shirt icon + Phase 15 reference. Type-check and lint clean. The student sidebar entry from Plan 14-06 now resolves successfully.
  </done>
</task>

</tasks>

<verification>
- File exists, compiles, lints
- Manual navigation to /wardrobe shows the placeholder (no 404)
- No assumptions made about top-level (protected) layout — placeholder is self-contained
</verification>

<success_criteria>
1. /wardrobe resolves to a real page, not a 404
2. The student sidebar Wardrobe entry from Plan 14-06 has a valid destination
3. Page is clearly marked as a placeholder so Phase 15 knows where to start
4. Brand consistency (cyan + navy, editorial header, standard card shadow)
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-inventory-crud/14-07-SUMMARY.md`. Document that this is a deliberate stub for NAV-01 satisfaction and will be replaced wholesale in Phase 15.
</output>
