---
phase: 03-query-scoping-super-admin
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/features/admin/components/analytics/CoachOverviewCards.tsx
  - src/features/admin/components/analytics/CoachDetailView.tsx
  - src/features/admin/components/analytics/RevenueBreakdownChart.tsx
  - src/app/(protected)/admin/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - "Super admin dashboard shows a coaches overview grid with each coach's status, hours booked, students, and earnings"
    - "Super admin can click a coach card to drill into that coach's details (upcoming lessons, student roster)"
    - "Revenue breakdown shows per-coach revenue with platform-wide totals and payout calculations"
    - "Dashboard gracefully shows existing content when no coaches are found"
    - "Existing dashboard sections (OverviewCards, RevenueChart, StudentActivityChart) remain fully intact and unmodified"
  artifacts:
    - path: "src/features/admin/components/analytics/CoachOverviewCards.tsx"
      provides: "Coaches grid with stats cards"
      min_lines: 50
    - path: "src/features/admin/components/analytics/CoachDetailView.tsx"
      provides: "Individual coach drill-down view"
      min_lines: 50
    - path: "src/features/admin/components/analytics/RevenueBreakdownChart.tsx"
      provides: "Per-coach revenue breakdown with payout calculations"
      min_lines: 50
    - path: "src/app/(protected)/admin/dashboard/page.tsx"
      provides: "Enhanced dashboard with coach sections"
      contains: "CoachOverviewCards"
  key_links:
    - from: "CoachOverviewCards.tsx"
      to: "api.admin.superAdmin.getCoachesOverview"
      via: "TRPC useQuery"
      pattern: "useQuery.*getCoachesOverview\\|admin\\.superAdmin\\.getCoachesOverview"
    - from: "CoachDetailView.tsx"
      to: "api.admin.superAdmin.getCoachDetail"
      via: "TRPC useQuery"
      pattern: "useQuery.*getCoachDetail\\|admin\\.superAdmin\\.getCoachDetail"
    - from: "RevenueBreakdownChart.tsx"
      to: "api.admin.superAdmin.getRevenueBreakdown"
      via: "TRPC useQuery"
      pattern: "useQuery.*getRevenueBreakdown\\|admin\\.superAdmin\\.getRevenueBreakdown"
---

<objective>
Build the super admin dashboard UI components: CoachOverviewCards (coaches grid), CoachDetailView (drill-down), and RevenueBreakdownChart (per-coach revenue), and integrate them into the existing admin dashboard page.

Purpose: The super admin needs a visual interface to monitor coach activity, drill into individual coaches, and review revenue splits. These components consume the queries built in Plan 03.

Output: Three new React components and an enhanced admin dashboard page.

NOTE: Coach drill-down uses React state (selectedCoachId) for simplicity. Deep-linkable URL params (useSearchParams) is a known enhancement deferred to a future phase -- not needed for initial super admin dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-scoping-super-admin/03-RESEARCH.md
@.planning/phases/03-query-scoping-super-admin/03-03-SUMMARY.md

Key reference files:
@src/app/(protected)/admin/dashboard/page.tsx (existing dashboard layout)
@src/features/admin/components/analytics/OverviewCards.tsx (existing pattern to follow)
@src/features/admin/components/analytics/RevenueChart.tsx (existing chart pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CoachOverviewCards and CoachDetailView components</name>
  <files>
    src/features/admin/components/analytics/CoachOverviewCards.tsx
    src/features/admin/components/analytics/CoachDetailView.tsx
  </files>
  <action>
    **CoachOverviewCards.tsx:**
    Create a client component ("use client") that:
    1. Calls `api.admin.superAdmin.getCoachesOverview.useQuery()` to fetch coaches data
    2. Shows a loading skeleton while fetching (follow OverviewCards.tsx pattern)
    3. Renders a grid of coach cards (2 cols on md, 3 on lg, 1 on mobile)
    4. Each card shows:
       - Coach name and email
       - Status badge: Active (green), Suspended (red), Inactive (gray)
       - Stats row: total lessons, active students, active time slots
       - Monthly earnings (after revenue split) formatted as currency
       - Total hours booked
    5. Each card is clickable -- on click, sets a `selectedCoachId` state
    6. When a coach is selected, renders `CoachDetailView` below the grid with the selected coachId
    7. Include a "Close" button on the detail view to deselect
    8. Use shadcn/ui Card, Badge components. Use Lucide icons (GraduationCap, Users, Clock, DollarSign, Calendar).
    9. If no coaches found, show a subtle message like "No coaches registered yet."
    10. Format currency with `$` prefix and 2 decimal places. Format hours to 1 decimal place.

    **CoachDetailView.tsx:**
    Create a client component ("use client") that:
    1. Accepts props: `{ coachId: string; onClose: () => void }`
    2. Calls `api.admin.superAdmin.getCoachDetail.useQuery({ coachId })` to fetch coach details
    3. Shows loading skeleton while fetching
    4. Renders three sections in a grid layout:
       **Profile section** (left, 1/3 width on lg):
       - Coach name, email
       - Bio (truncated if long)
       - Skills as comma-separated list
       - Revenue split percentage badge
       - Status badge
       **Upcoming lessons section** (right top, 2/3 width):
       - Table with columns: Date, Time, Student, Rink, Type
       - Show up to 20 upcoming lessons
       - Format dates with date-fns
       - If no upcoming lessons, show "No upcoming lessons"
       **Student roster section** (right bottom):
       - Simple list of student names and emails
       - Count of total students
       - If no students, show "No students assigned"
    5. Include a close/back button that calls `onClose`
    6. Use shadcn/ui Card, Table, Badge. Follow existing component patterns for styling.
    7. Use the same gradient card styling as the rest of the dashboard: `bg-gradient-to-br from-indigo-100/60 via-blue-50/70 to-slate-100/60 rounded-xl border border-indigo-200/50 shadow-lg`
  </action>
  <verify>
    Run `pnpm type-check` to confirm no TypeScript errors.
    Both files exist and export their components.
    Grep for `getCoachesOverview` in CoachOverviewCards.tsx -- should appear.
    Grep for `getCoachDetail` in CoachDetailView.tsx -- should appear.
  </verify>
  <done>
    CoachOverviewCards renders a grid of coach cards with status, stats, and earnings. Clicking a coach opens CoachDetailView with profile, upcoming lessons, and student roster. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RevenueBreakdownChart and enhance dashboard page</name>
  <files>
    src/features/admin/components/analytics/RevenueBreakdownChart.tsx
    src/app/(protected)/admin/dashboard/page.tsx
  </files>
  <action>
    **RevenueBreakdownChart.tsx:**
    Create a client component ("use client") that:
    1. Calls `api.admin.superAdmin.getRevenueBreakdown.useQuery()` (default date range = current month)
    2. Shows loading skeleton while fetching (follow RevenueChart pattern)
    3. Renders a summary section with platform-wide totals:
       - Total Revenue (before split)
       - Total Coach Payouts
       - Platform Revenue
       Format all as currency.
    4. Below totals, render a table of per-coach breakdown:
       - Columns: Coach Name, Revenue Split %, Total Revenue, Coach Payout, Platform Share, Lessons
       - Sort by total revenue descending
       - Use alternating row colors for readability
    5. If no revenue data (all zeros), show a subtle empty state message
    6. Use shadcn/ui Card, Table components
    7. Match the dashboard's visual style with gradient card borders

    **Dashboard page.tsx enhancement:**
    Add three new sections to the existing dashboard page, placed BELOW the existing content:
    1. **Coaches Overview section** -- between "Student Activity" and page bottom:
       ```
       <div className="space-y-6">
         <div className="flex items-center justify-between">
           <h2 className="text-2xl font-semibold tracking-tight">Coaches</h2>
         </div>
         <ErrorBoundary>
           <Suspense fallback={<LoadingSkeleton />}>
             <CoachOverviewCards />
           </Suspense>
         </ErrorBoundary>
       </div>
       ```
    2. **Revenue Breakdown section** -- after Coaches Overview:
       ```
       <div className="space-y-6">
         <div className="flex items-center justify-between">
           <h2 className="text-2xl font-semibold tracking-tight">Revenue Breakdown</h2>
         </div>
         <div className="p-2 bg-gradient-to-br from-indigo-100/60 via-blue-50/70 to-slate-100/60 rounded-xl border border-indigo-200/50 shadow-lg">
           <ErrorBoundary>
             <RevenueBreakdownChart />
           </ErrorBoundary>
         </div>
       </div>
       ```
    3. Import the new components using dynamic imports (same pattern as RevenueChart):
       ```
       const CoachOverviewCards = dynamic(() => import("@/features/admin/components/analytics/CoachOverviewCards").then(...), { loading: () => <LoadingSkeleton /> });
       const RevenueBreakdownChart = dynamic(() => import("@/features/admin/components/analytics/RevenueBreakdownChart").then(...), { loading: () => <LoadingSkeleton /> });
       ```

    **IMPORTANT:** Preserve ALL existing dashboard content exactly as-is. Only ADD new sections below. Do NOT modify the Overview Cards, Revenue Overview, Quick Actions, or Student Activity sections.
  </action>
  <verify>
    Run `pnpm type-check` to confirm no TypeScript errors.
    RevenueBreakdownChart.tsx exists and exports component.
    Dashboard page.tsx imports and renders CoachOverviewCards and RevenueBreakdownChart.
    Grep for `CoachOverviewCards` in dashboard/page.tsx -- should appear.
    Grep for `RevenueBreakdownChart` in dashboard/page.tsx -- should appear.

    **NEGATIVE VERIFICATION (existing dashboard integrity):**
    Grep for `OverviewCards` in dashboard/page.tsx -- MUST still appear (existing component preserved).
    Grep for `RevenueChart` in dashboard/page.tsx -- MUST still appear (existing component preserved).
    Grep for `StudentActivityChart` in dashboard/page.tsx -- MUST still appear (existing component preserved).
    Grep for `PendingApprovals` in dashboard/page.tsx -- MUST still appear (existing component preserved).
    If ANY of these four existing imports are missing after modification, the task has FAILED. Restore and retry.
  </verify>
  <done>
    RevenueBreakdownChart shows per-coach revenue breakdown with payout calculations. Admin dashboard page includes Coaches Overview and Revenue Breakdown sections below existing content. All existing dashboard content is preserved (OverviewCards, RevenueChart, StudentActivityChart, PendingApprovals all still present). TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Super admin dashboard with three new sections:
    1. Coaches Overview grid showing each coach's status, hours booked, students, and earnings
    2. Coach drill-down view (click any coach card) showing profile, upcoming lessons, and student roster
    3. Revenue Breakdown table showing per-coach revenue with payout calculations and platform totals
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Log in as admin/super admin
    3. Navigate to /admin/dashboard
    4. Scroll down past the existing Overview, Revenue, and Student Activity sections
    5. Verify "Coaches" section shows a grid of coach cards (at least Yura should appear)
    6. Click on a coach card -- verify the detail view opens showing profile info, upcoming lessons, and student list
    7. Click the close button on the detail view -- verify it closes
    8. Scroll to "Revenue Breakdown" section -- verify the table shows per-coach revenue with split calculations
    9. Verify no visual regressions in the existing dashboard sections (Overview, Revenue, Student Activity)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the dashboard layout/data</resume-signal>
</task>

</tasks>

<verification>
After all tasks are complete:
1. `pnpm type-check` passes with zero errors
2. Three new component files exist in src/features/admin/components/analytics/
3. Admin dashboard page renders all new sections
4. **Negative check:** Existing dashboard components (OverviewCards, RevenueChart, StudentActivityChart, PendingApprovals) are all still imported and rendered in dashboard/page.tsx
5. Visual verification confirms coaches data is displayed correctly
</verification>

<success_criteria>
- CoachOverviewCards shows grid of coaches with status, stats, and earnings (SADM-01)
- CoachDetailView shows individual coach profile, lessons, and students (SADM-02)
- RevenueBreakdownChart shows per-coach revenue with platform totals and payout calculations (SADM-03)
- All new sections are added BELOW existing dashboard content
- Existing dashboard content is unmodified (OverviewCards, RevenueChart, StudentActivityChart, PendingApprovals all preserved)
- TypeScript compiles without errors
- Visual verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-scoping-super-admin/03-04-SUMMARY.md`
</output>
