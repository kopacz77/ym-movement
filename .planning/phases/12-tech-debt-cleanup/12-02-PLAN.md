---
phase: 12-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tests/helpers/test-utils.ts
  - tests/role-guards.spec.ts
  - tests/authentication.spec.ts
autonomous: true

must_haves:
  truths:
    - "test-utils.ts exports only 4 functions: testData, loginAsSuperAdmin, loginAsAdmin, generateTestEmail"
    - "All 6 role-guard redirect tests in role-guards.spec.ts use test() not test.fixme()"
    - "Both protected-route redirect tests in authentication.spec.ts use test() not test.fixme()"
    - "The sign-out test in authentication.spec.ts runs without test.skip()"
    - "All 9 previously fixme'd/skipped tests pass when executed"
  artifacts:
    - path: "tests/helpers/test-utils.ts"
      provides: "Cleaned test utilities with only active exports"
      exports: ["testData", "loginAsSuperAdmin", "loginAsAdmin", "generateTestEmail"]
    - path: "tests/role-guards.spec.ts"
      provides: "Role guard tests with no fixme markers"
      contains: "test(\"student redirected"
    - path: "tests/authentication.spec.ts"
      provides: "Auth tests with no fixme markers and sign-out test enabled"
      contains: "test(\"should redirect unauthenticated"
  key_links:
    - from: "tests/role-guards.spec.ts"
      to: "proxy.ts"
      via: "proxy redirects tested by E2E assertions"
      pattern: "toHaveURL.*\\/auth\\/login|toHaveURL.*\\/student\\/dashboard|toHaveURL.*\\/coach\\/dashboard"
    - from: "tests/authentication.spec.ts"
      to: "src/components/layout/AppSidebar.tsx"
      via: "sign-out button found by test selector"
      pattern: "button:has-text.*Sign Out"
---

<objective>
Remove 18 orphaned exports from test-utils.ts and re-enable all 9 fixme'd/skipped tests now that proxy.ts and the sign-out button are in place.

Purpose: Complete the tech debt cleanup by removing dead code from test utilities and activating all deferred tests so the full E2E suite runs without fixme/skip markers.
Output: Clean test-utils.ts with 4 exports + 9 re-enabled tests across 2 spec files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tech-debt-cleanup/12-RESEARCH.md
@.planning/phases/12-tech-debt-cleanup/12-01-SUMMARY.md
@tests/helpers/test-utils.ts
@tests/role-guards.spec.ts
@tests/authentication.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned exports from test-utils.ts</name>
  <files>tests/helpers/test-utils.ts</files>
  <action>
  Rewrite `tests/helpers/test-utils.ts` to contain ONLY the 4 actively-used exports. Delete everything else.

  **KEEP these 4 exports (used by authentication.spec.ts and e2e-complete-flow.spec.ts):**
  1. `testData` object (lines 8-51) -- keep ALL entries (admin, coach, coach2, coach3, coach4, student, rink)
  2. `loginAsSuperAdmin` function (lines 56-62)
  3. `loginAsAdmin` alias (line 65) -- backward compat alias for loginAsSuperAdmin
  4. `generateTestEmail` function (lines 341-343)

  **DELETE these 18 exports:**
  - `loginAsStudent` (lines 70-76)
  - `loginAsCoach` (lines 81-87)
  - `navigateToCoachPage` (lines 92-95)
  - `approveCoach` (lines 100-108)
  - `createStudentAccount` (lines 122-171)
  - `navigateToAdminPage` (lines 176-179)
  - `navigateToStudentPage` (lines 184-187)
  - `createTimeSlot` (lines 192-217)
  - `bookLesson` (lines 222-238)
  - `approveStudent` (lines 243-254)
  - `verifyPayment` (lines 259-270)
  - `testResponsiveDesign` (lines 275-303)
  - `waitForApiCalls` (lines 308-323)
  - `clearTestData` (lines 328-336)
  - `generateFutureDateTime` (lines 348-355)
  - `takeScreenshot` (lines 360-365)
  - `assertNoConsoleErrors` (lines 370-382)
  - `mockApiResponse` (lines 387-395)
  - `testFormValidation` (lines 400-415)

  **Also update the import statement:**
  - Change `import { expect, type Page } from "@playwright/test";` to `import { type Page } from "@playwright/test";`
  - The `expect` import is only used by `assertNoConsoleErrors` and `testFormValidation`, which are being deleted.

  The resulting file should be approximately 50-60 lines (down from 415).
  </action>
  <verify>
  - `grep -c "export" tests/helpers/test-utils.ts` returns 4 (testData, loginAsSuperAdmin, loginAsAdmin, generateTestEmail)
  - `grep "clearTestData" tests/helpers/test-utils.ts` returns nothing (stub removed)
  - `grep "import.*expect" tests/helpers/test-utils.ts` returns nothing (expect import removed)
  - `pnpm type-check` passes
  </verify>
  <done>test-utils.ts contains exactly 4 exports (testData, loginAsSuperAdmin, loginAsAdmin, generateTestEmail) with no orphaned functions or stubs</done>
</task>

<task type="auto">
  <name>Task 2: Unfixme redirect tests and unskip sign-out test</name>
  <files>tests/role-guards.spec.ts, tests/authentication.spec.ts</files>
  <action>
  **role-guards.spec.ts -- unfixme 6 tests:**

  1. Change all 6 `test.fixme(` to `test(` in the redirect test blocks:
     - Line 15: `test.fixme("student redirected from /admin/dashboard` -> `test("student redirected from /admin/dashboard`
     - Line 21: `test.fixme("student redirected from /coach/dashboard` -> `test("student redirected from /coach/dashboard`
     - Line 31: `test.fixme("coach redirected from /admin/dashboard` -> `test("coach redirected from /admin/dashboard`
     - Line 41: `test.fixme("unauthenticated user redirected from /admin/dashboard` -> `test("unauthenticated user redirected from /admin/dashboard`
     - Line 47: `test.fixme("unauthenticated user redirected from /student/dashboard` -> `test("unauthenticated user redirected from /student/dashboard`
     - Line 53: `test.fixme("unauthenticated user redirected from /coach/dashboard` -> `test("unauthenticated user redirected from /coach/dashboard`

  2. Remove the FIXME block comment at the top of the file (lines 7-10) that explains the middleware bug. Replace with a brief note that these tests verify proxy.ts redirects.

  3. Remove inline FIXME comments inside each test (e.g., `// FIXME: Middleware not redirecting in Next.js 16 -- student stays on /admin/dashboard`).

  **authentication.spec.ts -- unfixme 2 tests + fix sign-out test:**

  4. Change 2 `test.fixme(` to `test(` in the Protected Routes section:
     - Line 167: `test.fixme("should redirect unauthenticated users to login from admin"` -> `test("should redirect unauthenticated users to login from admin"`
     - Line 172: `test.fixme("should redirect unauthenticated users from student dashboard"` -> `test("should redirect unauthenticated users from student dashboard"`

  5. Change 1 `test.fixme(` to `test(` in the Role-based Access section:
     - Line 183: `test.fixme("should prevent student from accessing admin dashboard"` -> `test("should prevent student from accessing admin dashboard"`

  6. Remove the FIXME block comments at lines 161-164 and 179-180 that explain the middleware issue.

  7. In the Logout test (lines 122-157), the current implementation gracefully skips if the sign-out button is not found (line 155: `test.skip(true, "Sign out button not found in current layout")`). Now that the sign-out button exists in the sidebar, this test should find it. No code changes needed to the test itself -- the `test.skip` is conditional and will only trigger if the button is truly not found. The test is already structured to work correctly with the new sidebar button.

  WHY unfixme AFTER proxy fix: The tests assert on redirect behavior. If proxy.ts is not working, removing fixme would cause 8 test failures. Plan 12-01 must complete first (proxy.ts exists and is the active interceptor).
  </action>
  <verify>
  - `grep -c "test.fixme" tests/role-guards.spec.ts` returns 0
  - `grep -c "test.fixme" tests/authentication.spec.ts` returns 0
  - `grep -c "FIXME" tests/role-guards.spec.ts` returns 0
  - `grep -c "FIXME" tests/authentication.spec.ts` returns 0
  - `pnpm type-check` passes
  </verify>
  <done>All 6 role-guards.spec.ts redirect tests use test() instead of test.fixme(), all 3 authentication.spec.ts fixme'd tests use test() instead of test.fixme(), no FIXME comments remain in either file</done>
</task>

</tasks>

<verification>
1. `tests/helpers/test-utils.ts` has exactly 4 exports (grep count)
2. Zero `test.fixme` calls remain in `role-guards.spec.ts` and `authentication.spec.ts`
3. Zero `FIXME` comments remain in `role-guards.spec.ts` and `authentication.spec.ts`
4. `pnpm type-check` passes across all modified test files
5. The sign-out test in authentication.spec.ts no longer has a hardcoded skip -- it conditionally skips only if button is truly missing (which it won't be after Plan 01)
</verification>

<success_criteria>
- test-utils.ts is clean: 4 exports, no orphaned functions, no stubs
- 9 previously deferred tests are now active (6 role-guards + 3 authentication)
- No FIXME or fixme markers remain in either spec file
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-tech-debt-cleanup/12-02-SUMMARY.md`
</output>
