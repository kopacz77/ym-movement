---
phase: 05-student-multi-coach-booking
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/student/components/booking/CoachBrowse.tsx
  - src/features/student/components/booking/CoachProfileCard.tsx
  - src/features/student/components/booking/BookingCalendar.tsx
  - src/features/student/components/booking/BookingDialog.tsx
  - src/app/(protected)/student/book/page.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees a grid of coach cards when they navigate to /student/book"
    - "Student can click a coach card to view that coach's available time slots"
    - "Student can go back to coach selection from the calendar view"
    - "BookingDialog displays which coach the lesson is with"
    - "BookingDialog shows prices that reflect the coach's pricing (via getStudentPricing with coachId)"
  artifacts:
    - path: "src/features/student/components/booking/CoachBrowse.tsx"
      provides: "Coach discovery grid component"
      min_lines: 40
    - path: "src/features/student/components/booking/CoachProfileCard.tsx"
      provides: "Individual coach card with profile info"
      min_lines: 30
    - path: "src/features/student/components/booking/BookingCalendar.tsx"
      provides: "Modified calendar that accepts coachId prop"
      contains: "coachId"
    - path: "src/features/student/components/booking/BookingDialog.tsx"
      provides: "Modified dialog showing coach name and coach-aware pricing"
      contains: "coachName"
    - path: "src/app/(protected)/student/book/page.tsx"
      provides: "Two-step booking flow: coach selection then calendar"
      contains: "selectedCoach"
  key_links:
    - from: "src/app/(protected)/student/book/page.tsx"
      to: "src/features/student/components/booking/CoachBrowse.tsx"
      via: "conditional render based on selectedCoach state"
      pattern: "selectedCoach.*CoachBrowse"
    - from: "src/features/student/components/booking/CoachBrowse.tsx"
      to: "api.student.coachBrowse.getBrowsableCoaches"
      via: "TRPC useQuery"
      pattern: "coachBrowse\\.getBrowsableCoaches"
    - from: "src/features/student/components/booking/BookingCalendar.tsx"
      to: "api.student.availability.getAvailableTimeSlots"
      via: "coachId passed in query input"
      pattern: "coachId.*getAvailableTimeSlots"
---

<objective>
Build the two-step student booking flow: browse coaches first, then view the selected coach's calendar and book.

Purpose: Students need to discover available coaches, see their profiles and pricing, select one, then see only that coach's available time slots. This replaces the current direct-to-calendar flow with a coach-first experience (BOOK-01).

Output: CoachBrowse grid component, CoachProfileCard component, modified BookingCalendar accepting coachId, modified BookingDialog showing coach name, and updated book page managing the two-step flow state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-student-multi-coach-booking/05-RESEARCH.md
@.planning/phases/05-student-multi-coach-booking/05-01-SUMMARY.md

Key files to read before starting:
@src/features/student/components/booking/BookingCalendar.tsx
@src/features/student/components/booking/BookingDialog.tsx
@src/app/(protected)/student/book/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoachBrowse and CoachProfileCard components</name>
  <files>
    src/features/student/components/booking/CoachBrowse.tsx
    src/features/student/components/booking/CoachProfileCard.tsx
  </files>
  <action>
1. Create `src/features/student/components/booking/CoachProfileCard.tsx`:
   - Define a `CoachProfile` interface: `{ id: string; name: string | null; bio: string | null; photoUrl: string | null; skills: string[]; certifications: string | null; yearsExperience: number | null; privateLessonPrice: number | null; groupLessonPrice: number | null; choreographyPrice: number | null; competitionPrepPrice: number | null; availableSlots: number; }`
   - Export the `CoachProfile` type.
   - Props: `{ coach: CoachProfile; onSelect: (coach: CoachProfile) => void }`
   - Use shadcn/ui Card, Badge, Avatar (from @/components/ui/).
   - Layout: Avatar at top-left (use initials from coach.name as AvatarFallback, AvatarImage from coach.photoUrl if present), name as h3 font-semibold, years experience as text-sm text-muted-foreground.
   - If bio exists, show truncated (line-clamp-2) in text-sm text-muted-foreground.
   - Show skills as Badge variant="secondary" with text-xs, wrapped in flex flex-wrap gap-1.
   - Bottom section: "From $X/hr" (calculate the lowest non-null price among privateLessonPrice, groupLessonPrice, choreographyPrice, competitionPrepPrice) on the left. Badge variant="outline" with "{availableSlots} slots available" on the right.
   - The entire card should be clickable. Use `onClick={() => onSelect(coach)}` on the Card. Add `cursor-pointer hover:shadow-md transition-shadow` classes.
   - Use `User` icon from lucide-react for the avatar fallback icon if no initials available.

2. Create `src/features/student/components/booking/CoachBrowse.tsx`:
   - "use client" directive.
   - Props: `{ onSelectCoach: (coach: CoachProfile) => void }`
   - Import CoachProfile type from `./CoachProfileCard`.
   - Use `api.student.coachBrowse.getBrowsableCoaches.useQuery()` to fetch coaches.
   - Show loading state with "Finding available coaches..." text.
   - Show empty state if no coaches returned: "No coaches are currently available."
   - Render a responsive grid of CoachProfileCard: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`.
   - Each card passes `onSelect={onSelectCoach}`.
  </action>
  <verify>
Run `pnpm type-check` to confirm both new components compile. Verify:
- CoachProfileCard exports the CoachProfile type
- CoachBrowse imports from the coachBrowse TRPC router (api.student.coachBrowse.getBrowsableCoaches)
  </verify>
  <done>
- CoachProfileCard renders coach info with avatar, name, bio, skills badges, pricing, availability count
- CoachBrowse fetches coaches via TRPC and renders a grid of CoachProfileCards
- Both components compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Two-step booking flow -- modify BookingCalendar, BookingDialog, and book page</name>
  <files>
    src/features/student/components/booking/BookingCalendar.tsx
    src/features/student/components/booking/BookingDialog.tsx
    src/app/(protected)/student/book/page.tsx
  </files>
  <action>
1. Modify `src/features/student/components/booking/BookingCalendar.tsx`:
   - Add props to the component: `{ coachId: string; coachName: string }`. The component is currently a no-props component wrapped in memo. Change `BookingCalendarComponent()` to accept props `{ coachId, coachName }: { coachId: string; coachName: string }`.
   - Pass `coachId` to the `getAvailableTimeSlots` query input alongside the existing startDate, endDate, rinkId, _cache params. Update the cacheKey to include coachId: `${dateRange.start.getTime()}-${dateRange.end.getTime()}-${selectedRink}-${coachId}`.
   - Pass `coachName` to `BookingDialog` as a new prop.
   - Update the memo wrapper: `const BookingCalendar = memo(BookingCalendarComponent)` -- keep existing export pattern.

2. Modify `src/features/student/components/booking/BookingDialog.tsx`:
   - Add `coachName?: string` and `coachId?: string` to `BookingDialogProps`.
   - Display coach name in the time slot details section (the bg-muted/50 card). Add a new row AFTER the MapPin/location row:
     ```tsx
     {coachName && (
       <div className="flex items-center gap-2">
         <User className="h-4 w-4 text-muted-foreground" />
         <span>Coach: {coachName}</span>
       </div>
     )}
     ```
     Import `User` from lucide-react.
   - Update the pricing query: Change `api.student.profile.getStudentPricing.useQuery({ studentId })` to `api.student.profile.getStudentPricing.useQuery({ studentId, coachId })` so the pricing reflects the coach's rates via the updated waterfall.

3. Rewrite `src/app/(protected)/student/book/page.tsx`:
   - Keep "use client", keep ApprovalGuard and ErrorBoundary wrapping.
   - Add state: `const [selectedCoach, setSelectedCoach] = useState<CoachProfile | null>(null)`.
   - Import CoachBrowse and CoachProfile from the booking components.
   - Import BookingCalendar dynamically (keep existing dynamic import pattern for code splitting).
   - When `selectedCoach` is null: render `<CoachBrowse onSelectCoach={setSelectedCoach} />`.
   - When `selectedCoach` is set:
     a. Render a header bar showing the selected coach name and a "Change Coach" button (Button variant="outline" size="sm" onClick={() => setSelectedCoach(null)}).
     b. Render `<BookingCalendar coachId={selectedCoach.id} coachName={selectedCoach.name || "Coach"} />`.
   - Page title: When no coach selected, "Choose Your Coach". When coach selected, "Book a Lesson with {coachName}".

IMPORTANT: Do NOT create a separate booking flow. Modify the existing BookingCalendar and BookingDialog. The anti-pattern from research says: do NOT duplicate logic.
IMPORTANT: Keep the existing rink filter within the calendar. Flow is: select coach -> select rink (existing) -> see slots -> book.
  </action>
  <verify>
Run `pnpm type-check` to confirm all modified components compile. Check that:
- BookingCalendar accepts coachId and coachName props
- BookingDialog accepts coachName and coachId props
- book/page.tsx manages the two-step state correctly
- The dynamic import of BookingCalendar still works
  </verify>
  <done>
- /student/book page shows coach grid when no coach selected
- Clicking a coach card transitions to that coach's calendar view with "Change Coach" option
- BookingCalendar passes coachId to the availability query, filtering slots to selected coach only
- BookingDialog shows coach name and uses coach-aware pricing
- Existing rink selection within the calendar is preserved
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check` passes with zero errors
2. Navigate to /student/book -- see coach grid (requires coaches in database)
3. Click a coach -- see calendar filtered to that coach's time slots
4. Click "Change Coach" -- return to coach grid
5. Select a time slot -- BookingDialog shows coach name and coach-aware price
6. Complete booking -- lesson created with correct coachId and price
</verification>

<success_criteria>
- Student sees coach cards at /student/book before seeing any calendar
- Selecting a coach filters the calendar to only that coach's time slots
- BookingDialog displays coach name and prices reflecting coach-specific rates
- "Change Coach" button returns student to coach selection
- No duplicate booking flow components -- existing BookingCalendar and BookingDialog are modified, not replaced
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-multi-coach-booking/05-02-SUMMARY.md`
</output>
